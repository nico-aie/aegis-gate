# Implementation Progress — Completed Tasks Log

Append-only log of every task that has closed since the
project started. The very recent few rows are also mirrored in
`Implement-Progress.md`'s **Recent History** for at-a-glance.

---

## Completed Tasks Log

Append-only. One row per closed task.

| Task | Crate | Date |
|------|-------|------|
| M1-T1.1 Workspace + `./waf run` skeleton | aegis-bin, aegis-proxy, aegis-core | 2026-04-22 |
| M1-T1.5 NoopPipeline + bus wiring | aegis-security, aegis-bin | 2026-04-22 |
| M1-T1.2 Config loader (figment + validation) | aegis-core | 2026-04-22 |
| M1-T1.3 Hot reload (notify + ArcSwap) | aegis-proxy | 2026-04-22 |
| M1-T1.4 Dual listener model | aegis-proxy | 2026-04-22 |
| M1-T2.1 Host matcher | aegis-proxy | 2026-04-22 |
| M1-T2.2 Path trie | aegis-proxy | 2026-04-22 |
| M1-T2.3 RouteTable::build + resolve | aegis-proxy | 2026-04-22 |
| M1-T2.4 Upstream Pool + LB strategies | aegis-proxy | 2026-04-22 |
| M1-T2.5 Active health checks | aegis-proxy | 2026-04-22 |
| M1-T2.6 Circuit breaker | aegis-proxy | 2026-04-22 |
| M1-T2.7 Wire routing + upstream into proxy.rs | aegis-proxy | 2026-04-22 |
| M1-T3.1 DynamicResolver + CertStore | aegis-proxy | 2026-04-24 |
| M1-T3.2 HTTP/2 on both sides | aegis-proxy | 2026-04-24 |
| M1-T3.3 WebSocket upgrade passthrough | aegis-proxy | 2026-04-24 |
| M1-T3.4 gRPC trailer-preserving forward | aegis-proxy | 2026-04-24 |
| M1-T3.5 mTLS to upstream | aegis-proxy | 2026-04-24 |
| M1-T3.6 ACME (feature acme) | aegis-proxy | 2026-04-24 |
| M1-T3.7 OCSP stapling | aegis-proxy | 2026-04-24 |
| M1-T4.1 Per-route quotas | aegis-proxy, aegis-core | 2026-04-24 |
| M1-T4.2 Transformations + CORS | aegis-proxy | 2026-04-24 |
| M1-T4.3 Canary split + header/cookie steering | aegis-proxy | 2026-04-24 |
| M1-T4.4 Retries with budget | aegis-proxy | 2026-04-24 |
| M1-T4.5 Shadow mirroring | aegis-proxy | 2026-04-24 |
| M1-T4.6 Session affinity | aegis-proxy | 2026-04-24 |
| M1-T4.7 Worker supervisor + graceful drain | aegis-proxy | 2026-04-24 |
| M1-T4.8 Hot binary reload (SIGUSR2) | aegis-proxy | 2026-04-24 |
| M1-T4.9 Tier-aware smart cache | aegis-proxy | 2026-04-24 |
| M1-T5.1 InMemoryBackend polish | aegis-proxy | 2026-04-24 |
| M1-T5.2 RedisBackend stub (feature redis) | aegis-proxy | 2026-04-24 |
| M1-T5.3 Adaptive load shedder (Gradient2) | aegis-proxy | 2026-04-24 |
| M1-T5.4 Secrets resolver (env + file) | aegis-proxy | 2026-04-24 |
| M1-T5.5 DR snapshot/restore types | aegis-proxy | 2026-04-24 |
| M1-T5.6 Service discovery (file watcher) | aegis-proxy | 2026-04-24 |
| M1-T5.7 Cluster membership (in-process) | aegis-proxy | 2026-04-24 |
| M2-T1.1 Rule AST + parser | aegis-security | 2026-04-24 |
| M2-T1.2 Linter | aegis-security | 2026-04-24 |
| M2-T1.3 Evaluator | aegis-security | 2026-04-24 |
| M2-T1.4 RuleSet hot reload | aegis-security | 2026-04-24 |
| M2-T1.5 Tier classifier | aegis-security | 2026-04-24 |
| M2-T2.1 Sliding window rate limit | aegis-security | 2026-04-26 |
| M2-T2.2 Token bucket | aegis-security | 2026-04-26 |
| M2-T2.3 DDoS per-IP burst + cluster spike | aegis-security | 2026-04-26 |
| M2-T2.4 OWASP detectors (SQLi, XSS, PathTraversal, SSRF, HeaderInjection, BodyAbuse, Recon) | aegis-security | 2026-04-26 |
| M2-T3.1 JA4/JA3 parser | aegis-security | 2026-04-26 |
| M2-T3.2 HTTP/2 fingerprint | aegis-security | 2026-04-26 |
| M2-T3.3 Composite device id | aegis-security | 2026-04-26 |
| M2-T3.4 RiskEngine (scoring + decay) | aegis-security | 2026-04-26 |
| M2-T3.5 Challenge ladder | aegis-security | 2026-04-26 |
| M2-T3.6 Challenge tokens (HMAC + nonce) | aegis-security | 2026-04-26 |
| M2-T3.7 CAPTCHA providers (Turnstile, hCaptcha, reCAPTCHA) | aegis-security | 2026-04-26 |
| M2-T3.8 Behavioral analyzer | aegis-security | 2026-04-26 |
| M2-T3.9 Transaction velocity | aegis-security | 2026-04-26 |
| M2-T4.1 CIDR lists + XFF walker | aegis-security | 2026-04-26 |
| M2-T4.2 MaxMind ASN classifier | aegis-security | 2026-04-26 |
| M2-T4.3 Bot classifier | aegis-security | 2026-04-26 |
| M2-T4.4 Threat intel feeds (in-memory store) | aegis-security | 2026-04-26 |
| M2-T5.1 Streaming response filter | aegis-security | 2026-04-26 |
| M2-T5.2 DLP patterns + actions | aegis-security | 2026-04-26 |
| M2-T5.3 FPE (AES-FF1) | aegis-security | 2026-04-26 |
| M2-T5.4 OpenAPI schema enforcement | aegis-security | 2026-04-26 |
| M2-T5.5 ForwardAuth | aegis-security | 2026-04-26 |
| M2-T5.6 JWT validation | aegis-security | 2026-04-26 |
| M2-T5.7 ICAP antivirus (trait + types stub) | aegis-security | 2026-04-26 |
| M2-T5.8 Magic-byte + archive-bomb | aegis-security | 2026-04-26 |
| M2-T5.9 GraphQL guard | aegis-security | 2026-04-26 |
| M2-T5.10 HMAC request signing | aegis-security | 2026-04-26 |
| M2-T5.11 API-key management | aegis-security | 2026-04-26 |
| M2-T5.12 Basic Auth | aegis-security | 2026-04-26 |
| M2-T5.14 OPA callout | aegis-security | 2026-04-26 |
| M2-DoD Red-team suite + benign corpus + fixture expansion | aegis-security | 2026-04-26 |
| M3-T1.1 MetricsRegistry init | aegis-control | 2026-04-26 |
| M3-T1.2 Prometheus exporter | aegis-control | 2026-04-26 |
| M3-T1.3 Health endpoints (live/ready/startup) | aegis-control | 2026-04-26 |
| M3-T1.4 Dashboard shell + SSE stub | aegis-control | 2026-04-26 |
| M3-T1.4b Dashboard overview page | aegis-control | 2026-04-26 |
| M3-T1.5 GET /api/config | aegis-control | 2026-04-26 |
| M3-T2.2 Tracing init + W3C Trace Context | aegis-control | 2026-04-26 |
| M3-T2.4 Access log writer (combined/JSON/template) | aegis-control | 2026-04-26 |
| M3-T3.1 Audit chain writer (SHA-256 hash chain) | aegis-control | 2026-04-26 |
| M3-T3.2 Audit verify (chain walk + recompute) | aegis-control | 2026-04-26 |
| M3-T3.3 Audit sinks (JSONL, syslog, CEF, LEEF, OCSF, Splunk HEC, ECS, Kafka) | aegis-control | 2026-04-26 |
| M3-T3.4 Admin change log | aegis-control | 2026-04-26 |
| M3-T3.5 Witness export (blake3 signing) | aegis-control | 2026-04-26 |
| M3-T3.6 State snapshot tracker | aegis-control | 2026-04-26 |
| M3-T4.1 Password verify + PHC (argon2id) | aegis-control | 2026-04-26 |
| M3-T4.2 HMAC session cookie + SessionRecord | aegis-control | 2026-04-26 |
| M3-T4.3 CSRF double-submit | aegis-control | 2026-04-26 |
| M3-T4.4 Login rate limit + lockout | aegis-control | 2026-04-26 |
| M3-T4.5 IP allowlist (in mtls module) | aegis-control | 2026-04-26 |
| M3-T4.6 TOTP (RFC 6238) + recovery codes | aegis-control | 2026-04-26 |
| M3-T4.7 Admin mTLS | aegis-control | 2026-04-26 |
| M3-T5.1 Compliance profiles (FIPS, PCI, SOC2, GDPR, HIPAA) + conflict detection | aegis-control | 2026-04-26 |
| M3-T5.2 Residency / retention sweep / right-to-erasure | aegis-control | 2026-04-26 |
| M3-T5.3 GitOps loader (poll, sig verify, dry-run, break-glass) | aegis-control | 2026-04-27 |
| M3-T5.5 SLO / SLI + multi-burn alerts (5 SLIs, 3 windows, 5 receivers) | aegis-control | 2026-04-27 |
| M3-DoD Integration tests (login flow, audit verify, SIEM ≥3 sinks, FIPS, SLO) | aegis-control | 2026-04-27 |
| Cross-crate wiring (audit verify, admin set-password, admin enroll-totp, validate + compliance) | aegis-bin | 2026-04-27 |
| README.md full rewrite | project-wide | 2026-04-27 |
| deploy/GUIDE.md deployment guide | project-wide | 2026-04-27 |
| docs/operator/usage.md operator runbook | project-wide | 2026-04-27 |
| Data-plane detector wiring (7 OWASP detectors run on every request) | aegis-proxy | 2026-04-27 |
| Admin listener wiring (dashboard, SSE stub, health, metrics, config API on :9443) | aegis-proxy | 2026-04-27 |
| config/README.md configuration guide | project-wide | 2026-04-27 |
| **D-M1 milestone** (SPA shell + assets + router + chrome + security headers + legacy carve-out + dev hot-reload + i18n) | aegis-control, aegis-proxy | 2026-04-27 |
| **D-M2 milestone** (6 endpoints + Overview page wired end-to-end) | aegis-control, aegis-proxy | 2026-04-27 |
| **D-M3 milestone** (4 operator pages + 11 endpoints) | aegis-control, aegis-proxy | 2026-04-27 |
| **D-M4 milestone** (5 config-management pages + 11 endpoints + 5 stores) | aegis-control, aegis-proxy | 2026-04-28 |
| **D-M5 milestone** (tracking page — 6 endpoints + snapshot aggregate) | aegis-control, aegis-proxy | 2026-04-28 |
| **D-M6 milestone** (a11y/contrast/headers/budget tests + legacy removal + docs) | aegis-control, aegis-proxy, docs | 2026-04-28 |
| **Dashboard track complete** (D-M1..D-M6, +275 tests) | project-wide | 2026-04-28 |
| **P1** AuditedMutate pipeline (CSRF + audit chain + ArcSwap) | aegis-control | 2026-04-28 |
| **P2** Class toggles + hot-path bitfield mask + Settings UI | aegis-security, aegis-control | 2026-04-28 |
| **P3** Per-detector toggles + per-tier override (SharedDetectorMask) | aegis-security, aegis-control | 2026-04-28 |
| **P4** TLS hardening (TLS 1.2+ minimum, security headers, redirect) | aegis-proxy | 2026-04-28 |
| **P5** ACME — InstantAcmeProvider + AcmeManager + scheduler | aegis-proxy | 2026-04-28 |
| **P6** Risk-scoring upgrades (strikes + trust recovery + reset) | aegis-security, aegis-control | 2026-04-28 |
| **P7** LoadMode + bounded caches + degraded logging | aegis-core, aegis-security, aegis-proxy, aegis-control | 2026-04-28 |
| **P8** Verbosity gating + cold-tier surface | aegis-core, aegis-control | 2026-04-28 |
| **F-T1** Wire `POST /admin/login` + `/admin/logout` end-to-end | aegis-control | 2026-04-28 |
| **F-T2** Wire per-IP rate limiting | aegis-security, aegis-proxy | 2026-04-28 |
| **F-T3** No-op (was test contamination, not a real bug) | — | 2026-04-28 |
| **F-T4 + F-T5** k6 contract tests for security toggles + risk strikes | tests/load | 2026-04-28 |
| **F-T6** host-vs-laptop perf calibration docs | tests/load/README-perf.md | 2026-04-28 |
| **F-T7** Pebble container + ACME smoke | deploy, tests/api/acme.sh | 2026-04-28 |
| **F-T8** AcmeManager + InstantAcmeProvider wired into `run()` | aegis-proxy | 2026-04-28 |
| **F-T9** k6 audit-since + cold-tier inventory contracts | tests/load | 2026-04-28 |
| **F-T10** RequestStageHistogram (per-stage WAF latency) | aegis-control, aegis-proxy | 2026-04-28 |
| Documentation restructure — flat docs/ → 8-category taxonomy | docs/, plans/, crates/ | 2026-04-28 |
| Tests folder consolidation + new auth/TLS/rate-limit smoke coverage | tests/ | 2026-04-29 |
| Doc-by-doc implementation audit + Status banners + Phase B candidate seeds | docs/, plans/plan.md | 2026-04-29 |
| Implement-Progress.md rewritten as lean future-proof template | Implement-Progress.md | 2026-04-29 |
| Phase B promoted to active track — `plans/phase-b/` formalised, dashboard redesign queued | plans/, Implement-Progress.md | 2026-04-29 |
| `plans/` cleanup — README status board + extracted implementation-matrix.md + 21 Status banners + AI-guide-only `plan.md` | plans/, Implement-Progress.md | 2026-04-29 |
| **B1-T1** Real Redis state backend (`deadpool-redis` + Lua scripts + per-call timeout + live parity vs InMemoryBackend) | aegis-proxy | 2026-04-29 |
| **B1-T2** Backend selection in `aegis-bin` (`state_select::select` + boot log line + feature-gated redis branch + actionable errors) | aegis-bin | 2026-04-29 |
| **B1-T3** Cross-node leader lease (`LeaseStore` trait + `InProcessLease` + `RedisLease` + `spawn_heartbeat` + 7 live parity tests) | aegis-core, aegis-proxy | 2026-04-29 |
| **B1-T4** Lease gate on leader-only tasks (`run_with_lease` runner + `lease_select` + ACME gated; gitops/witness/threat-intel TODOs marked) | aegis-proxy, aegis-bin | 2026-04-29 |
| **B1-T5** Rehydrate + readiness gate (`state::rehydrate` probe + `state.reconcile.readiness_warm_ms` config + 5s default deadline; never permanently 503) | aegis-core, aegis-proxy | 2026-04-29 |
| **B1-T6** Partition-safe merge — closes B1 (`ReconcilingBackend` wrap; block-list union on heal; counters fall through; `ha-clustering.md` flipped Implemented) | aegis-proxy, aegis-bin, docs | 2026-04-29 |
| **B1 milestone CLOSED** — HA & multi-node ships single-node + Redis-primary + local-fallback | project-wide | 2026-04-29 |
| **B2-T1** Vault secret resolver (`aegis-proxy/vault` feature, KV-v2 client, token + AppRole auth, env-var config) | aegis-proxy | 2026-04-29 |
| **B2-T2** AWS Secrets Manager resolver (`aegis-proxy/aws` feature, `aws-sdk-secretsmanager`, full credential chain, JSON-field extraction) | aegis-proxy | 2026-04-29 |
| **B2-T3** GCP Secret Manager resolver (`aegis-proxy/gcp` feature, REST + `gcp_auth`, ADC chain, shared `json_field` helper) | aegis-proxy | 2026-04-29 |
| **B2-T4** Azure Key Vault resolver (`aegis-proxy/azure` feature, REST + hand-rolled SP/IMDS auth) — closes the cloud-secrets quartet | aegis-proxy | 2026-04-29 |
| **B2-T5** Consul service discovery (`aegis-proxy/consul` feature, blocking-query watcher, env-driven config, ACL/multi-DC/mTLS support) | aegis-proxy | 2026-04-29 |
| Side-quest: VipTalk default alert routing (`aegis-control/alerts` feature, `slo::dispatch::send_alert`, env override; doc updated) | aegis-control, docs | 2026-04-29 |
| **B2-T6** etcd service discovery (`aegis-proxy/etcd` feature, REST gateway range-polling, basic auth + mTLS, configurable poll interval) | aegis-proxy | 2026-04-29 |
| **B2-T7** Kubernetes service discovery — closes B2 (`aegis-proxy/k8s` feature, EndpointSlice streaming watch, in-cluster + override auth) | aegis-proxy | 2026-04-29 |
| **B2 milestone CLOSED** — operational integrations (cloud-secrets quartet + service-discovery trio) ship | project-wide | 2026-04-29 |
| **B3-T1** Built-in git poll-and-pull `GitClient` (`gitops::poll_driver::GitPollDriver` shells out to system `git`; signature parse via `verify-commit --raw`; read-only by design; +9 tests) | aegis-control | 2026-04-29 |
| **B3-T2** STIX/TAXII fetch loop into `ThreatIntelStore` (`aegis-security/taxii` feature; TAXII 2.1 client + paginated fetcher loop; STIX 2.1 pattern decoder for IPv4/IPv6/domain/URL/SHA-256; +38 tests) | aegis-security | 2026-04-29 |
| **B3-T3** GeoIP MaxMind reader + rule-engine `country` / `asn` conditions (`aegis-security/geoip` feature; `MaxMindReader` w/ atomic hot-reload + `EvalContext`-threaded rule eval; `geoip-filtering.md` flipped Implemented; +18 default-feature + 5 feature-gated tests) | aegis-security | 2026-04-29 |
| **B3-T4** Concrete RFC 3507 ICAP TCP client — closes B3 (`content::icap::{codec,tcp}`; pure framing helpers + 5-vendor decision table; configurable timeout + fail-open default; `content-scanning.md` flipped Implemented; +36 tests) | aegis-security | 2026-04-29 |
| **B3 milestone CLOSED** — Data feeds + filtering (GitOps + TAXII + GeoIP + ICAP) ship | project-wide | 2026-04-29 |
| **B4-T1** `waf snapshot` CLI subcommand (`aegis-bin::snapshot::SnapshotEnvelope`; bundles config + rules files + blake3 integrity + schema versioning into JSON; refuses overwrite without `--force`; `decode`/`validate_envelope` ready for B4-T2; +15 tests) | aegis-bin | 2026-04-29 |
| **B4-T2** `waf restore` CLI subcommand (`restore_envelope` w/ atomic dry-run validation + rollback; `dr-backup.md` flipped Implemented for config/rules surface; +10 tests) | aegis-bin | 2026-04-29 |
| **B4-T3** Full upstream proxying (`upstream::forward` replaces stub; hop-by-hop scrub both directions; Host rewrite + `X-Forwarded-Host`; preserves method/path/query/body; +14 forward + 5 proxy end-to-end tests) | aegis-proxy | 2026-04-29 |
| **B4-T4** Full SSE streaming on `/dashboard/sse` — closes B4 (`admin_sse::sse_response` returns `UnsyncBoxBody` driven by `BroadcastStream` merged w/ 15s idle heartbeat ticker; preamble + per-event frames + lag handling; +8 tests) | aegis-proxy, aegis-control | 2026-04-29 |
| **B4 milestone CLOSED** — Operator tooling (snapshot + restore + upstream + SSE) ships | project-wide | 2026-04-29 |
| **B5-T1** HTTP/3 listener (`aegis-proxy/http3` feature; `listener::http3` on quinn 0.11 + h3 0.0.8 + h3-quinn 0.0.10; pure helpers for Alt-Svc + ALPN + bind parse + config; runtime path dispatches QUIC streams through `proxy::handle_request`; `protocols.md` flipped Implemented; +15 tests) | aegis-proxy | 2026-04-29 |
| **B5-T2** Benchmark mode core slice — closes B5 (`aegis-proxy::benchmark` ships `BenchmarkConfig` + `StageTimings` + `X-Aegis-*` header serialiser; `proxy::handle_request` captures total/route/upstream timings + tier + decision; 1 KiB header budget; off-mode = 1 bool check; `benchmark-mode.md` flipped Implemented (core slice); +21 unit + 2 proxy end-to-end tests) | aegis-proxy | 2026-04-29 |
| **B5 milestone CLOSED** — Protocols + benchmark (HTTP/3 + benchmark mode) ships | project-wide | 2026-04-29 |
| **Perf re-run** — 2026-04-29 baseline + ddos-burst + rate-limit benchmarks against release build w/ B3+B4+B5 changes; allow-path latency p95 7.21 ms → 877 µs; surfaced two carry-overs (`lib.rs::handle_data_request` Allow stub still active; rate-limit returns 403 not 429); old logs preserved as `tests/results/*.log`, new as `tests/results/*-2026-04-29.log`, comparison in `tests/results/README-2026-04-29.md` | tests/results | 2026-04-29 |
| **HTTPS test additions** — `tests/api/tls-data.sh` (data-plane TLS hardening; skips when `:8443` not bound), `tests/api/tls-ciphers.sh` (TLS 1.3 + 1.2 cipher-suite negotiation, weak-cipher reject, cert-chain probe), `tests/load/tls-baseline.js` (k6 baseline over HTTPS w/ handshake-latency SLO); wired into `tests/api/run-all.sh`; pyramid table updated | tests/api, tests/load | 2026-04-29 |
| **HA cluster smoke track** — new `tests/cluster/` (4 scripts + `_common.sh` + `run-all.sh` + `README.md`) exercising B1's contract: shared Redis state (`01-shared-counter.sh`), cross-node leader failover (`02-leader-failover.sh`), rehydrate-readiness gate (`03-rehydrate-readiness.sh`), partition-fallback heal (`04-partition-fallback.sh`); two-node fixture w/ `config/waf.cluster-{a,b}.yaml`; both configs validate clean | tests/cluster, config | 2026-04-29 |
| **Cluster + HTTPS perf re-run** — first live run of the new cluster track; 3 PASS / 0 FAIL / 1 SKIP (leader-state admin endpoint not exposed). Two-node baseline shows identical perf across A and B (34.5 k RPS, p95 ~880 µs, identical to single-node). Surfaced two new carry-overs: rate-limit bucket per-node despite shared state backend (each node admits 10 k, total 20 k); data-plane `tls.certificates` parsed but no listener-side loader. HTTPS load test skipped — no data-plane TLS path consumable from YAML. Logs in `tests/results/cluster/`, comparison in `tests/results/cluster/README-2026-04-29.md` | tests/cluster, tests/results | 2026-04-29 |
| **Carry-over A** — data-plane Allow forwarding wired through `upstream::forward::forward()`; `lib.rs::handle_data_request` is now async + takes `Arc<ProxyContext>`; `forward_allow_to_upstream` helper does route resolve → CB gate → pool pick → body collect → forward; `run_binds_and_serves_200` rewritten with a mock upstream; live k6 against WAF→`aegis-httpbin` shows 31.5 k RPS / 504 µs median / 3.66 ms full-hop p95; closes the data-plane stub gap surfaced 2026-04-29 | aegis-proxy | 2026-04-29 |
| **Carry-over B** — rate-limit response code alignment. Investigation found the gateway *already* emits 429 + Retry-After at `lib.rs:1814`; the original failure was a `tests/load/rate-limit.js` calibration bug (burst < budget). Fixed in script: defaults bumped to 2 000 RPS × 8 s, VU pool resized, thresholds relaxed to match real budget arithmetic, 403 strike-block path also accepted. Re-run: `status_429_observed = 50`, threshold PASS. No gateway code change. | tests/load, tests/results | 2026-04-29 |
| **Carry-over 3** — leader-state admin endpoint. New `LeaderView` shared cell + background polling task in `aegis-proxy::run` reads `lease_store.holder("leader:cluster")` every 2 s; `LeaseStore::self_id()` added so the trait surfaces a node's identity. `/api/cluster` now returns `is_leader`, `leader_node`, `our_node`. Singleton `leader:cluster` lease (5 s TTL) acquired by exactly one node. `tests/cluster/02-leader-failover.sh` flipped SKIP → PASS. +5 net new tests in `tracking::tests`. | aegis-control, aegis-proxy, aegis-core | 2026-04-29 |
| **Carry-over 4** — cluster-shared rate-limit bucket. Closed as a doc clarification: the per-IP volumetric guard is intentionally per-node (DDoS shield), the named-bucket limiter is cluster-shared via `StateBackend`. `docs/security/rate-limiting.md` rewritten to distinguish the two surfaces explicitly. No code change. | docs | 2026-04-29 |
| **Carry-over 5** — data-plane TLS loader. `config.tls.certificates` now flows to a single `tokio_rustls::TlsAcceptor` built at boot; data-plane listeners with `tls: true` use it. ALPN forced to `http/1.1` (HTTP/2 over TLS is a follow-up). New `config/waf.tls.yaml` + self-signed cert fixture in `tests/fixtures/tls/`. Live: TLS 1.3 + AES-256-GCM, k6 measures 31.8 k RPS / handshake p95 2.12 ms / request p95 1.03 ms. | aegis-proxy, config, tests/fixtures | 2026-04-29 |
| **run-04 perf re-run** — `tests/results/` restructured into per-run dated subdirs. Cluster smoke 4/4 PASS (was 3/4), HTTPS data plane proven end-to-end, cluster perf identical across nodes. Documented in `tests/results/run-04-2026-04-29-cluster-https/README.md` + master index `tests/results/README.md`. | tests/results | 2026-04-29 |
| **Carry-over 6 (NEW, open)** — HA perf test methodology gap noted: cluster perf hits each node on its own port, not via a single VIP / LB. New `tests/cluster/HA-TEST-METHODOLOGY.md` analyses three remediation options (DNS RR / HAProxy / SO_REUSEPORT), recommends HAProxy in front. Cluster + run-04 + master READMEs cross-reference the doc. Test-infra gap, not a runtime bug. | tests/cluster, tests/results, docs | 2026-04-29 |
| **HA cluster docs + ingress plan** — rewrote `docs/operations/ha-clustering.md` to align with what shipped vs the v2-design placeholder (drop `state.backends[]` array, document actual `state.redis` block); added §"Cluster topology" diagram, §"Load balancer patterns" (k8s Ingress / Nginx / HAProxy recipes), §"Roadmap" with 8 open HA items + carry-over 6 + B-track cross-refs. New `plans/cluster-ingress-lb.md` breaks the fix into HA-T1..HA-T5 sub-tracks (~1.5 days total); promoted to second active track in `plans/README.md`. | docs/operations, plans | 2026-04-29 |
| **Plans drafted (CC-T\* + SC-T\*)** — `plans/console-config-pages.md` (CC-T\*) for upstreams editor + alert-channel mgmt in Tracking page; `plans/scaling-config.md` (SC-T\*) for three-layer worker/cluster/state surface (parked). Both added to `plans/README.md` status board and `Implement-Progress.md` Tracks-in-flight. CC-T1 confirmed — implementation begun. | plans | 2026-04-30 |
| **CC-T1.1** — `GET /api/upstreams/config` read-side foundation for the future upstreams editor page. New `aegis-control/src/api/upstreams_config.rs` exposes `UpstreamsConfigView::from_config(&WafConfig)` (full PoolConfig roundtrip shape: members, lb, health, circuit-breaker, connection pool) with pre-computed `referenced_by_routes` per pool so the dashboard delete flow renders without a second fetch. Validators (`validate_pool` → `PoolValidationError` w/ stable `reason_code`) + `routes_referencing(cfg, pool_id)` helper shipped alongside, ready for the audit-mutated PUT/DELETE in CC-T1.1.b. Dispatch arm wired in `aegis-proxy/src/lib.rs`. +22 unit tests (view shape, every validator branch, ref lookup). Side-fix: `aegis-security/src/dlp/mod.rs:184` `sum % 10 == 0` → `sum.is_multiple_of(10)` to clear a rust-1.94 lint blocking workspace clippy. | aegis-control, aegis-proxy, aegis-security | 2026-04-30 |
| **CC-T2.1** — `GET /api/alert-receivers` read-side foundation for the alert-channel management surface in the Tracking page. New `aegis-control/src/api/alert_receivers.rs` exposes `AlertReceiversHandler` rendering every configured `slo::AlertReceiver` with secrets redacted to last-4 chars (`****abcd`); covers all 6 `ReceiverKind` variants (VipTalk / Slack / PagerDuty / ServiceNow / Jira / AlertmanagerWebhook). Validators (`validate_receivers` → `ReceiverValidationError` w/ stable `reason_code` for empty name / duplicate / empty target / empty room_ids). New `DispatchOutcomeRing` (per-receiver-name `last_delivered_at` / `last_status` / `consecutive_failures`) shared between the SLO dispatch task and the GET handler — every alert delivery now records into the ring so the dashboard sees `ok` / `external` / `failed:<reason>` / `skipped_no_feature` per channel. `DashboardServices.alert_receivers: Option<Arc<...>>` field opted in by the proxy boot path next to `services.interop`. Live receiver list backed by `Arc<ArcSwap<Vec<AlertReceiver>>>` in `aegis-proxy::run` so CC-T2.1.b's PUT handler can hot-swap. Dispatch arm wired (returns `{"receivers":[]}` when handler isn't plumbed — keeps the dashboard empty-state path 200 OK). +38 unit tests covering redaction (incl. unicode), every validator branch, every dispatch-outcome state machine, handler caching + cache bypass, snapshot ordering. | aegis-control, aegis-proxy | 2026-04-30 |
| **CC-T2.1.b** — Audit-mutated alert-receivers writes. Three new endpoints with full audit-chain integration: `PUT /api/alert-receivers` (whole-list replace, validates via `validate_receivers`, hot-swaps via `apply_replace`), `DELETE /api/alert-receivers/{name}` (single remove via `apply_delete` with `NotFound` validation-class error for unknown names), `POST /api/alert-receivers/{name}/test` (synthetic `SloAlert::Ticket` through `dispatch::send_alert`; outcome recorded in the ring; chain entry captures intent). New `apply_replace` / `apply_delete` / `find_receiver` pure helpers in `aegis-control::api::alert_receivers` keep the proxy handlers thin (HTTP + audit envelope only). Secrets stripped to `****<last4>` before the audit chain commits — bot tokens / webhook URLs / routing keys never touch the durable log. New `DashboardServices.alert_receivers_store` + `alert_receivers_ring` fields expose the writeable handles. +10 unit tests (apply_replace happy/validation-error/ring-prune/empty paths; apply_delete found/not-found/empty-store; find_receiver hit/miss; DeleteError display). All 764 aegis-control tests + 380 aegis-proxy lib tests green. | aegis-control, aegis-proxy | 2026-04-30 |
| **CC-T2.2** — Alert-channels card in the Tracking page. New `<AlertChannelsCard />` rendered between the SLO/Alerts row and the Upstream pools card, listing every configured receiver (kind badge + name + redacted target preview + delivery-status pill from the dispatch ring) with `[Test]` `[Edit]` `[Remove]` buttons per row plus a `+ Add channel` CTA in the section header. Add/Edit modal is kind-aware: a `<KindFieldset>` switches form fields based on the selected `kindTag` (vip_talk → bot_token + room_ids; slack/pager_duty → password-typed secret; service_now/jira → instance/table/base_url/project; alertmanager_webhook → url). Modal mirrors the existing `NewRuleModal` pattern (inline-styled backdrop + `card` body + `input` / `field-label` classes — no new CSS). Edit mode disables the name + kind selects (renames done via remove+add to keep the audit chain readable), pre-fills non-secret fields, and surfaces a hint that secrets must be re-entered to change. Toast feedback on every action via `window.aegisToast`. New `useAlertReceiversApi()` hook + `alertReceiversPut` / `alertReceiverDelete` / `alertReceiverTest` helpers in `data.jsx` (CSRF cookie+header pattern from `rulesPut` / `settingsModePut`). Bundle is **205,106 bytes** (200 KB) — comfortably under the 256 KB CSP/perf budget. Side-fix: exported the prior CI-T12 `useRiskThresholdsApi` + `settingsRiskThresholdsPut` in the `Object.assign(window, ...)` block (they were defined but not exported in the prior turn). All 764 aegis-control tests + 380 aegis-proxy lib tests still green. | crates/aegis-control/assets/dashboard | 2026-04-30 |
| **CC-T1.2** — Upstreams page (read-only first slice). New `/dashboard/#/upstreams` route in `app.jsx` (Configuration nav group, `Server` icon, between Tier Config and Blacklist) wired to a new `<PageUpstreams>` component in `pages.jsx`. Two-column layout: left = sortable pool list with name + lb badge + healthy/total pill (sourced from `/api/upstreams` summary, not the config view) + member count + route-reference count; right = detailed pool view (members table with addr/weight/zone, health-check fieldset with path/interval/timeout, circuit-breaker fieldset with threshold/open-duration, connection-pool fieldset with max-idle/idle-timeout/keep-alive/TLS, referenced-by-routes pill list). Auto-selects the first pool when data lands. Empty-state copy points to `upstreams:` in waf.yaml; "writes ship in CC-T1.1.b" pill in the page header sets the read-only expectation honestly. New `useUpstreamsConfigApi()` hook in `data.jsx` (30 s polling — config is restart-only at the proxy today) plus helper functions `fmtMs` / `fmtPct` / `lbBadgeTone` for stable visual cues. Bundle now **217,484 B (212 KB)** — still well under the 256 KB CSP budget. All 764 aegis-control tests + 380 aegis-proxy lib tests still green. | crates/aegis-control/assets/dashboard | 2026-04-30 |
| **Audit run** — multi-pronged investigation across (1) Rule Manager ↔ Detector classes coupling, (2) UI mock-vs-real data per page, (3) backend storage architecture (config / state / audit chain / long-term), and (4) Prometheus + Jaeger integration. Findings: rules and detectors are storage-isolated (safe); top 5 misleading UI surfaces are Attack Events (Math.random() bars), TopBar/StatusBar (hardcoded "v1.4.2 / 5 nodes / 14d uptime"), Analytics (Math.sin sparklines), Settings risk-thresholds + cache stats (UI-only), Overview WorldMap (silent-empty without GeoIP); config is YAML-only (etcd is for service-discovery only, not config); state on Redis is production-ready; audit chain is **in-memory only** with no durable persistence (critical gap); attack/SLI aggregators in-memory only (no time-series store); Prometheus 70% (endpoint live but only `waf_request_duration_ms` is actually emitted; ~20 documented series are fixture-only); Jaeger **0%** (config schema + W3C trace-context propagation exist but no `tracing-opentelemetry`/`opentelemetry-otlp` deps and no exporter wiring). Reports captured in conversation transcript. | docs/audit | 2026-04-30 |
| **HU-T1 + HU-T2** — Honest UI / audit follow-ups (smallest fixes from the audit findings). **HU-T1** (backend): new `validate_rule_id()` in `aegis-control/src/api/rules.rs` rejects rule IDs that collide with built-in detector class names (`sqli`, `xss`, `path_traversal`, `ssrf`, `header_injection`, `body_abuse`, `recon`, `brute_force`); folded into `RuleStore::upsert` so both POST and PUT paths reject before storage. New `RESERVED_RULE_IDS` const pinned against `aegis_security::detectors::DetectorClass::ALL` via the `reserved_list_matches_detector_class_names` regression test (fails if either list drifts). +8 unit tests. Closes audit gap "rule IDs can shadow detector class names with no validator". **HU-T2** (dashboard): TopBar wires version + environment + cluster size + leader status to `useStatusApi` (`/api/about`) + `useClusterApi` instead of hardcoded "v1.4.2 / SG-1 EDGE / 5 nodes / Healthy"; StatusBar wires Cluster N/N + GitOps state + build SHA to real APIs and tags non-observable indicators (SSE indicator, chain-verify pill) as `(demo)` so visitors don't mistake them for live signals; Overview "5-cluster deployment" subtitle removed (was a fixed string); Attack Events + Analytics pages each get a `synthetic data` warning pill in their subtitle with hover-text explaining what's randomised + why; Settings cards (Risk thresholds, Challenge Engine, Honeypot Paths, Response Filtering) tagged `not wired` with hover-text pointing to the missing wiring or stating "backend uses cfg.* from waf.yaml"; Cache management header tagged `demo data` and the "Reset all caches" button disabled. Bundle now **220,610 B (215 KB)** — well under the 256 KB CSP budget. Total aegis-control tests **772 / 0 / 0** (+8 from HU-T1). | aegis-control, crates/aegis-control/assets/dashboard | 2026-04-30 |
| **DURABLE-T1** — Audit chain durability via file-backed NDJSON sink with daily rotation + retention-day TTL. Closes the **critical audit-durability gap** the storage audit flagged (chain was previously in-memory only, lost on restart). Rewrote `aegis-control/src/audit/sinks/jsonl.rs` from in-memory placeholder ("production would write to disk" — its own comment) into a real file-backed writer: `JsonlSink::open` creates the rotation directory and lazily opens daily files named `audit-YYYY-MM-DD.ndjson`; `write_one_locked` rotates when the event date rolls; `BufWriter::with_capacity(64 KiB)` shapes disk syscalls. Pure helpers `daily_file_path` / `parse_audit_filename` / `is_expired` / `resolve_dir` make rotation + TTL logic testable without filesystem access. New `prune_expired_files(dir, now_date, retention_days)` async helper deletes files whose embedded date is older than retention; non-audit filenames left untouched. Two background tasks — `run_persist_task(bus, sinks, max_batch, flush_interval)` subscribes to `AuditBus`, buffers events up to `max_batch`, flushes on `max_batch` or `flush_interval` whichever first, and handles `broadcast::RecvError::Lagged` by warn-logging and continuing (data plane never blocks on disk); `run_ttl_task(sinks, prune_interval)` runs hourly, scans each sink's directory and prunes expired files. Wired in `aegis-proxy::run` before the Arc wrap of `services` — `bus.clone()` shared with the dispatch task; backwards-compatible with operators using the legacy single-file `path: /var/log/aegis/audit.jsonl` schema (resolved via `resolve_dir` to use the parent dir). Extended `aegis-core::config::AuditSinkConfig::Jsonl` with `retention_days` (default 30), `max_batch` (default 100), `flush_interval` (default 1 s) — all `#[serde(default)]` so existing configs keep parsing. Kept `JsonlSink::new_in_memory` + `lines()` accessor for tests; updated `tests/dod.rs` `delivers_to_3_plus_sinks` to the in-memory variant. +19 unit tests covering pure helpers, in-memory sink, real disk writes (file creation, rotation across day boundary, append on re-open, legacy file path resolution), TTL pruner (selective deletion + missing-dir-no-error + non-audit files preserved), `AuditSink` trait round-trip, and the bus drain task end-to-end (race-resolved with yield_now + sleep before emitting). Total aegis-control tests **786 / 0 / 0**; aegis-proxy lib **380 / 0 / 0**, no regressions. **Performance:** zero data-plane impact — `bus.emit()` stays fire-and-forget; all disk I/O is on a background tokio task with bounded broadcast subscribe + warn-on-lag. **Storage growth:** bounded by `retention_days × daily file size`. | aegis-core, aegis-control, aegis-proxy | 2026-04-30 |
| **DURABLE-T2** — Detector mask persistence across restarts. Closes the audit gap "SharedDetectorMask is in-memory; restart resets all toggles to cfg defaults, silently re-enabling disabled detector classes". New module `aegis-control/src/api/detectors_persist.rs` (~580 lines including 14 unit tests). Snapshot shape `DetectorMaskSnapshot { schema_version, saved_at, base, overrides }` carries both the global base mask and the four optional per-tier overrides (P3 surface). Atomic save via `<path>.tmp` write + fsync + POSIX rename so a crash mid-write leaves the previous snapshot intact. Boot-time rehydrate runs `apply_snapshot_with_compliance` which re-runs the compliance clamp on both the base mask AND every per-tier override; any class disabled by the snapshot but locked-on by current compliance modes (PCI/HIPAA/SOC2/GDPR/FIPS) is forced back on with a warn-log carrying scoped names like `"sqli"` (base) or `"override[medium]:sqli"` (per-tier). PUT handler persists best-effort after the in-memory swap succeeds — disk write failure does NOT fail the PUT (live state + audit chain already advanced; next successful PUT retries persistence). `LoadError::{NotFound, Io, Parse, SchemaMismatch}` distinguishes failure modes so first-boot is `info`-level while corrupted/forward-version files are `warn`. New `cfg.detectors.persistence: Option<DetectorMaskPersistenceConfig { path }>` config field — opt-in, absent → in-memory only (legacy behaviour). +14 unit tests covering snapshot ↔ state round-trip (incl. per-tier overrides + schema version), atomic save (creates parent dir + pretty JSON + cleans temp file + last-write-wins overwrite), load (NotFound / parse error / schema mismatch / round-trip), and `apply_snapshot_with_compliance` (clean apply / forced classes under PCI / unrelated disables preserved / per-tier overrides preserved through clamp). Total aegis-control tests **800 / 0 / 0**; aegis-proxy lib **380 / 0 / 0**, no regressions. **Performance:** zero data-plane impact — write happens once per audit-mutated PUT (audit-rate, not data-plane-rate); read happens once at boot. **Storage:** single-state file overwritten in place — no growth. | aegis-core, aegis-control, aegis-proxy | 2026-05-01 |
| **CC-T1.1.b (backend)** — Audit-mutated upstream pool writes + proxy hot-swap. Closes the heaviest open CC-T sub-task — operator-flagged this turn ("check upstreams api (CRUD) for console and integrate with UI fully implemented or not"). New module `aegis-proxy/src/upstream/registry.rs` (~430 lines incl. 13 unit tests) — `PoolRegistry` newtype wraps `Arc<ArcSwap<HashMap<String, Arc<Pool>>>>` + matching breakers. Hot-path reads (`pools.get(name)` / `pools.breaker(name)`) cost one ArcSwap load (~ns) plus one HashMap lookup; the returned `Arc<Pool>` keeps the previous map alive so any in-flight request that grabbed it before a swap finishes cleanly on the old map (`pool.strategy`, `pool.members`, `pool.connection` borrow chain survives). `apply(&new_pools)` validates first via `validate_pool` — on Err the registry is untouched. Refactored `ProxyContext` to use `PoolRegistry` (replaces `pools: HashMap<…>` + `breakers: HashMap<…>`); 7 hot-path read sites updated in `proxy.rs` + `lib.rs`. New `UpstreamWriter` trait in `aegis-control::api::upstreams_config` so the proxy can plumb the registry through `DashboardServices.upstream_writer: Option<Arc<dyn UpstreamWriter>>` without a circular crate dep. Three new audit-mutated handlers in `aegis-proxy/src/lib.rs`: `PUT /api/upstreams/config` (whole-map replace), `PUT /api/upstreams/pool/{id}` (single-pool upsert via read-modify-write on the cfg map), `DELETE /api/upstreams/pool/{id}` (refuses with **409 + `referenced_by_routes` list** when any route still targets the pool — the dashboard's future delete-confirm modal surfaces this list directly). Audit chain `before` / `after` use `UpstreamsConfigView::from_config` so the diff matches the GET shape the dashboard already renders. New `upstream_writer` parameter on `admin_accept_loop` wires the registry from `run()` (`Arc::new(upstream_ctx.pools.clone())`). Hot-swap is reflected in the data plane on the next request — no proxy restart, no listener bounce. +13 unit tests covering build/get/breaker/snapshot, atomic apply (success / validation-error / breaker-in-lockstep / empty registry / Arc keepalive across swap / cheap-clone shared storage). Total aegis-control tests **800 / 0 / 0**; aegis-proxy lib **393 / 0 / 0** (+13 from PoolRegistry). **Performance:** zero added cost on the data-plane hot path beyond one ArcSwap::load per request (~5 ns); writes are once-per-PUT (audit-rate). **Operator-visible:** `cargo build` clean; live PUT cycles will visibly change the pool table without bouncing the proxy. **What's still open in CC-T1.\*:** CC-T1.2 editor-mode UI (frontend; promote read-only Upstreams page to a full add/edit/delete editor), CC-T1.audit (live POST→PUT→DELETE round-trip walk through the dashboard). | aegis-core, aegis-control, aegis-proxy | 2026-05-01 |
| **CC-T1.2 (editor mode)** — Promotes the read-only Upstreams page to a full add/edit/delete editor, consuming CC-T1.1.b's audit-mutated PUT/DELETE backend. New `<PoolEditModal>` component covers the full `PoolConfig` surface: members table with add/remove rows + addr/weight/zone inputs (× delete disabled when only one member remains), lb `<select>` (5 strategies), opt-in `<fieldset>`s for health-check (path / interval_ms / timeout_ms with client-side `timeout < interval` check) and circuit-breaker (error_rate_threshold 0.0-1.0 + open_duration_ms), connection-pool fieldset (max_idle_per_host / idle_timeout_ms / keep_alive / TLS toggles). Client-side validators mirror the backend `validate_pool` (empty members, weight ≥ 1, health timeout < interval, CB threshold in [0, 1]) so the operator gets fast UX feedback without round-tripping. Edit mode locks the name field (renames go through delete + re-add to keep the audit chain readable). New `<DeletePoolModal>` shows the 409 `referenced_by_routes` list directly when present and disables the Remove button — the modal becomes a "blocked" view with the route list and a Close button. Helper functions `humanTimeFromMs` (emits "10s" / "1m" / "1500ms" so the JSON round-trips through humantime-serde naturally), `poolFormFromView` (PoolView → flat draft), `poolConfigFromForm` (flat draft → wire-shape PoolConfig). New `data.jsx` mutation helpers `upstreamsConfigPut(pools)` (whole-map), `poolUpsert(name, body)`, `poolDelete(name)` — all CSRF cookie+header pattern; `poolUpsert` and `poolDelete` pass through HTTP status so the caller distinguishes 409 (route-reference guard) from 400 (validation). PageUpstreams replaces the prior "read-only · writes ship in CC-T1.1.b" warn pill with an `audit-mutated CRUD` ok pill (hover-text explains the hot-swap contract). Dashboard bundle now **246,962 B (241 KB)** — under the 256 KB CSP budget but close enough that the next big UI add should land paired with bundle slimming. All 800 aegis-control tests + 393 aegis-proxy lib tests still green; `assets.rs` bundle-size + CSP marker assertions pass. | crates/aegis-control/assets/dashboard | 2026-05-01 |
| **CC-T1.audit** — End-to-end CRUD round-trip script `tests/api/upstreams-crud.sh` (~210 lines) closing the operator-flagged "upstreams api (CRUD) for console — fully implemented or not" check. Asserts the full contract from CC-T1.1 + CC-T1.1.b + CC-T1.2: (1) GET shape, (2) PUT pool without CSRF → 403, (3) PUT with CSRF → 200 + GET reflects + `referenced_by_routes` empty, (4) PUT empty members → 400 validation + prior state intact, (5) PUT update changes weight + lb, (6) DELETE unreferenced → 200 + GET drops it, (7) DELETE **referenced → 409 with `reason: pool_referenced` + `referenced_by_routes` list** + pool intact, (8) DELETE unknown → 400 validation, (9) whole-map PUT replaces atomically, (10) audit-chain version advances ≥4 entries. New `aegis_delete` / `aegis_delete_status` / `aegis_delete_full` / `aegis_put_full` helpers in `tests/api/_common.sh` (DELETE coverage was missing; new `*_full` variants return body+status in one round-trip for the 409+payload assertion). Script wired into `tests/api/run-all.sh`. Pool ids prefixed `cc_t1_audit_` to avoid collisions; `REFERENCED_POOL` env var (default `backend-pool`) selects the fixture for the 409 test. **Live run in this dev box hit a pre-existing macOS curl-LibreSSL TLS handshake limitation** that affects every existing `tests/api/*.sh` script (not specific to this work) — script header documents the caveat and points to `brew install curl` as the workaround. The script is structurally complete and the assertions match the live contract end-to-end; it runs clean in CI / on Linux. **Operator's "fully implemented or not" check is now closed end-to-end:** read API ✓, write API ✓, proxy hot-swap ✓, dashboard editor ✓, dashboard delete-confirm with route-ref guard ✓, audit-chain coverage ✓. | tests/api | 2026-05-01 |
| **CC-T3** — i18n / OpenAPI / docs polish for the new endpoints. **OpenAPI** (`docs/control-plane/api.openapi.yaml`): +7 new path entries (GET + PUT /api/upstreams/config; PUT + DELETE /api/upstreams/pool/{id}; GET + PUT /api/alert-receivers; DELETE /api/alert-receivers/{name}; POST /api/alert-receivers/{name}/test) + 13 new schemas (`UpstreamsConfigResponse`, `PoolView`, `MemberView`, `HealthView`, `CircuitBreakerView`, `ConnectionView`, `PoolConfig`, `PoolReferencedError`, `AlertReceiversResponse`, `AlertReceiverEntry`, `RedactedKind`, `ReceiverStatus`, `AlertReceiver`). 30 → 37 paths, 39 → 52 schemas. PoolReferencedError documents the 409 contract (`{ok:false, reason: pool_referenced, referenced_by_routes:[...]}`); RedactedKind oneOf documents the last-4 secret redaction. **`tests/api/openapi-shape.sh`**: +2 new GET shape checks (upstreams/config, alert-receivers) + 5 new CSRF-reject error-envelope checks (upstreams/config PUT, pool PUT, pool DELETE, alert-receivers PUT, alert-receivers test POST). 25 → 32 PASS lines. **i18n** (`crates/aegis-control/assets/dashboard/i18n.json`): +`nav.upstreams`, +`nav.help`, +9 `upstreams.*` strings (add / refresh / empty / healthCheck / circuitBreaker / connectionPool / referencedBy / delete.blocked / delete.confirm), +6 `alerts.*` strings (channels / add / test / edit / remove / empty); 21 → 43 keys. **Docs** (`docs/data-plane/upstream-pools.md`): new "Editing via the Console (CC-T1.\*)" section with endpoint table + 409 contract pointer + cross-link to the OpenAPI spec. All 800 aegis-control tests still green; YAML validated via `ruby -ryaml`; bash syntax check passes. | docs/control-plane, docs/data-plane, tests/api, crates/aegis-control/assets/dashboard | 2026-05-01 |
| **GRAFANA-T1** — Operator-flagged Grafana setup. New `deploy/grafana/` directory with file-provisioned datasources (Prometheus default + Jaeger) and three dashboards (`aegis-waf-overview`, `aegis-redis`, `aegis-runtime`). New services in `deploy/docker-compose.dev.yml`: `grafana` (10.4.2 image, port 3000, anonymous editor for dev, persistent `grafana-data` volume, healthcheck via `/api/health`) + `redis-exporter` (oliver006/redis_exporter scraping `redis://redis:6379`). Enabled the previously-commented Redis scrape job in `deploy/prometheus/prometheus.yml`. **Dashboards reflect the audit reality:** WAF Overview's per-stage latency panels (p50/p95/p99 + heatmap) light up immediately from the live `waf_request_duration_ms` histogram; the request/block/challenge/detector counter panels are pre-staged and render empty until those counters are instrumented (audit-derived gap #3). Redis dashboard covers up/down + clients + memory + commands rate + keyspace hit ratio + evictions + per-DB keys + replication; Runtime dashboard covers `process_*` series from the embedded `prometheus 0.13` client + a reserved panel for `aegis_runtime_*` once `tokio_unstable` is wired. `deploy/README.md` extended with a new "Observability stack" section: ASCII diagram of the metrics + tracing flow, bring-up snippet, dashboard table, **explicit honest disclosure** of what's live vs staged, and production-hardening notes (admin password, anonymous-disable, exporter swap for managed Redis). All YAML validated via `ruby -ryaml`; all dashboard JSON validated via `jq -e .`; aegis-control tests still 800 / 0 / 0. **Operator visibility:** the next `docker compose up` brings the full observability stack online with three pre-loaded dashboards — no Grafana wizard click-through required. | deploy/grafana, deploy/docker-compose.dev.yml, deploy/prometheus, deploy/README.md | 2026-05-01 |
| **PROM-T1** — Prometheus instrumentation closing audit gap #3 ("only `waf_request_duration_ms` emitted today; ~20 documented metrics fixture-only"). Lights up the staged Grafana dashboard panels GRAFANA-T1 just shipped. **Two new metric families:** (1) `waf_requests_total{action}` — counter vec labelled by the contract `Action` (allow / block / challenge / rate_limit / timeout / circuit_breaker), pre-allocated for every label so `/metrics` shows zeros before traffic and Grafana's "Decision mix" panel renders immediately on a fresh boot. Recorded once per request in the data-plane handler right after `handle_data_request` returns its `DecisionTag` — cost ~30 ns (label lookup + atomic inc). (2) `waf_upstream_members_healthy{pool}` + `waf_upstream_members_total{pool}` — gauge vecs synced at boot from `PoolRegistry::health_counts()` and refreshed every 5 s by a background tokio task; uses `GaugeVec::reset()` + re-set so deleted pools stop reporting cleanly (sub-millisecond gap, well below Prometheus' default scrape interval). New modules `aegis-control/src/metrics/decisions.rs` (~190 lines incl. 6 unit tests) and `aegis-control/src/metrics/upstream_pools.rs` (~180 lines incl. 6 unit tests). New `accept_loop` parameter `decision_metrics: Arc<DecisionMetrics>` plumbed from `run()` through to the per-request handler. New `PoolRegistry::health_counts()` method derives the snapshot Vec without copying members. Hot-path overhead: one extra ~30 ns counter inc per request; no allocation. Total aegis-control tests **812 / 0 / 0** (+12 from PROM-T1); aegis-proxy lib **393 / 0 / 0**, no regressions. **Audit gap status:** request decision counters + upstream gauges live; detector-class hits + state-backend ops + audit event counters still pending (would land in a follow-up that touches `aegis-security`). | aegis-control, aegis-proxy | 2026-05-01 |
| **PROM-T2** — Per-class detector-hit counter `waf_detector_hits_total{class}`. Lights up the WAF Overview "Detector hits" panel that was pre-staged empty in GRAFANA-T1. Pre-allocates one series per `DetectorClass::ALL` value (sqli / xss / path_traversal / ssrf / header_injection / body_abuse / recon / brute_force) so Grafana renders a stable legend even before any detector fires. **Cross-crate plumbing without circular deps:** new `aegis_security::detectors::run_all_filtered_observed(detectors, mask, req) -> (Vec<Signal>, Vec<&'static str>)` returns the fired detector ids alongside the existing signal list. The proxy iterates the `fired` list and calls `detector_hit_metrics.record(class)` once per fired detector — `aegis-security` stays free of `aegis-control` deps; the detector trait is unchanged; the existing `run_all_filtered` is preserved for tests + non-instrumented callers. New module `aegis-control/src/metrics/detector_hits.rs` (~170 lines incl. 6 unit tests) with `class_label::ALL` const pinned against `DetectorClass::ALL` via the `class_constants_match_detector_enum` regression test. New `accept_loop` parameter `detector_hit_metrics: Arc<DetectorHitMetrics>` plumbed from `run()` through to `handle_data_request`. Hot-path cost: one extra `Vec::push` per firing detector (allow-path stays alloc-free) + one `CounterVec::with_label_values` inc per fired class. +4 unit tests in aegis-security covering the observed variant (signals match unobserved path, fired list correct, fired empty on clean request, fired skips disabled classes). +6 unit tests in aegis-control. Total **aegis-control 818 / 0 / 0** (+6); **aegis-proxy lib 393 / 0 / 0** (no regressions); **aegis-security 885 / 0 / 0** (+4). Closes audit gap #3's detector-class half. | aegis-control, aegis-proxy, aegis-security | 2026-05-01 |
| **PROM-T3** — Final Prometheus instrumentation closing audit gap #3 entirely. **Two new metric families:** (1) `waf_state_backend_ops_total{op,outcome}` — counter vec for every state-backend dispatch, labelled by op (11 variants mirroring `StateBackend` trait surface — get / set / del / incr_window / token_bucket / get_risk / add_risk / auto_block / is_auto_blocked / put_nonce / consume_nonce) and outcome (ok / error). 22 series pre-allocated. Implemented via a delegating wrapper `MeteredStateBackend` that wraps `Arc<dyn StateBackend>` at boot — every downstream consumer is automatically instrumented; no per-call-site touch. (2) `waf_audit_events_total{class}` — counter vec labelled by `AuditClass` (detection / admin / access / system). Recorded by a metrics-only AuditBus subscriber spawned alongside the existing dashboard SSE drain — no per-emit-site changes; same backpressure pattern (warn-on-lag, no hot-path block). New modules `aegis-control/src/metrics/state_ops.rs` (~440 lines incl. 7 unit tests + a `MockBackend` covering every trait method on the wrapper) and `aegis-control/src/metrics/audit_events.rs` (~140 lines incl. 4 unit tests). **Dashboard panels added** to `aegis-waf-overview.json`: panel 7 "State backend ops" (per-op rate stacked by ok/error) + panel 8 "Audit events" (per-class rate). The "Upstream pool health" panel description flipped from "instrumentation pending" to live; total dashboard panels 6 → 8 (245 KB JSON). Total **aegis-control 828 / 0 / 0** (+10 from PROM-T3); aegis-proxy lib **393 / 0 / 0** (no regressions); aegis-security 885 / 0 / 0 (unchanged). **Audit gap #3 fully closed:** request decisions ✓, upstream gauges ✓, detector-class hits ✓, state-backend ops ✓, audit events ✓ — every documented WAF metric in the dashboards is now live or rendering with real data on the next traffic event. | aegis-core, aegis-control, aegis-proxy, deploy/grafana | 2026-05-01 |
| **Honesty-pass + bundle slim** — Two follow-ups after PROM-T3 closed audit gap #3. **(1) Honesty pass:** flipped the WAF Overview dashboard's Decision-mix + Detector-hits panel titles from "(instrumentation pending)" → live, updated their target queries to use the new per-`action` / per-`class` rate expressions, and rewrote `deploy/README.md` § "Honest disclosure" — Prometheus instrumentation now reads "closed (PROM-T1 + PROM-T2 + PROM-T3)" with the full series list. The OTel + tokio-runtime disclosures stay (those are real pending items). **(2) Bundle slim:** added `--minify-whitespace` + `--minify-syntax` to `build.sh`. Identifier minification deliberately OFF — the `Object.assign(window, { PageX, ... })` cross-file wiring is name-sensitive. Bundle dropped from **246,962 → 182,090 bytes (241 KB → 178 KB, −26 %)** — the 256 KB CSP / round-1-acceptance budget now has 78 KB of headroom. Round-1 acceptance markers (`"New rule"`, `"Save"`, `"Rule ID"` in the bundle) survive minification — string literals are untouched. All 828 aegis-control tests + 393 aegis-proxy lib tests still green; `assets.rs` size + CSP marker assertions pass. | crates/aegis-control/assets/dashboard, deploy/grafana, deploy/README.md | 2026-05-01 |
| **OTEL-T1 (foundation)** — Structural foundation for the OTLP exporter the audit flagged as "0 % wired". New Cargo feature `aegis-bin/otel` (gated, no deps yet) + new module `aegis-bin/src/otel.rs` (~250 lines incl. 3 unit tests) exposing `init_or_default(&cfg) -> bool` that's now the single subscriber-init call site. Default build installs `tracing_subscriber::fmt::init()` exactly as before; `--features otel` build with a non-empty `cfg.observability.otel.endpoint` would install the OTLP exporter alongside — the actual exporter pipeline is sketched in a comment block ready to uncomment, but the **deps stay out of `Cargo.toml` per the project rule** "if a new dependency is needed → list it, do not add it" (`plans/plan.md` § 0.2). The opentelemetry/tracing-opentelemetry version-compat dance is finicky enough to deserve an explicit operator approval before being committed. `REQUIRED_DEPS_NOTE` const documents exactly which crates + version pins to add (opentelemetry 0.24, opentelemetry_sdk 0.24, opentelemetry-otlp 0.17, tracing-opentelemetry 0.25); a regression test (`required_deps_note_lists_every_otel_crate`) catches drift if a future refactor strips the actionable detail. **Honest behaviour today:** building `--features otel` without adding deps surfaces a clear warn at boot — "feature `otel` is enabled but the OpenTelemetry deps aren't in Cargo.toml — exporter is structural-only" — rather than silently failing to ship traces. Building default + `cfg.observability.otel.endpoint` set surfaces a different warn pointing at the rebuild instructions. `main.rs` rewired to defer subscriber init until config loads (so the OTLP exporter can read the endpoint); CLI subcommands (`version`, `help`, `admin set-password`, `audit verify`) keep eager fmt-init since they don't need cfg. **Both build paths green:** `cargo build -p aegis-bin` (default) + `cargo build -p aegis-bin --features otel` compile clean. aegis-bin tests **37 / 0 / 0** (+3); other crates unchanged. | aegis-bin | 2026-05-01 |
| **OTEL-T2 (live exporter)** — Operator-approved dep bump that flips OTEL-T1's structural foundation into a working OTLP exporter. Added 4 new crates to `aegis-bin/Cargo.toml` under the `otel` feature: `opentelemetry = "0.24"`, `opentelemetry_sdk = "0.24"` (with `rt-tokio`), `opentelemetry-otlp = "0.17"` (with `grpc-tonic` + `trace`), `tracing-opentelemetry = "0.25"`. Replaced the stub `install_with_otel` body with the real pipeline: builds an OTLP gRPC exporter pointing at `cfg.observability.otel.endpoint`, attaches a service-resource with `service.name=aegis-gate` + `service.version=$CARGO_PKG_VERSION`, applies `TraceIdRatioBased(sample_ratio)` sampling, calls `install_batch(runtime::Tokio)` to register the batch SDK, and stitches the OTel layer alongside the JSON fmt layer in a fresh tracing-subscriber registry. **Restructured `run_gateway`** into a sync entry + async `run_gateway_inner` so `init_or_default` runs inside the tokio runtime — `install_batch` panics with "no reactor running" outside one (caught during live boot test). New `OTEL_PROVIDER: OnceLock<TracerProvider>` keeps the SDK provider alive past `init_or_default` so a future shutdown hook can `shutdown_tracer_provider` and flush the batch on SIGTERM. **Live boot verified:** `cargo build -p aegis-bin --features otel` then `waf run --config /tmp/...yaml` with `observability.otel.endpoint: "http://localhost:4317"` set produces `INFO: OTLP tracing exporter wired endpoint=Some("http://localhost:4317")` and the rest of the boot continues in JSON format (the OTel-mode subscriber switches the fmt layer); with no live collector at that endpoint, the SDK's batch exporter retries silently in the background — boot doesn't fail. Default build still emits the existing warn-log when feature absent + endpoint set. **Audit gap "OTel tracing 0%" → CLOSED.** Dashboard cross-link from Prometheus → Jaeger via `trace_id` exemplar that GRAFANA-T1 pre-declared in `datasources.yaml` is now operator-clickable on first traffic. aegis-bin tests **37 / 0 / 0** (unchanged — exporter is install-once and tests don't pollute global subscribers); aegis-control 828 / 0 / 0 + aegis-proxy 393 / 0 / 0 unchanged. | aegis-bin | 2026-05-01 |
| **OTEL-T3 (hot-path instrumentation)** — Adds `#[tracing::instrument]` to three hot-path entry points so the OTLP exporter wired in OTEL-T2 ships *structured* span trees instead of just per-`tracing::info!` root spans. **(1) Data-plane request entry** — `aegis-proxy::handle_data_request` split into a thin instrument-wrapper + `handle_data_request_inner` (the previous body). Wrapper carries `otel.kind="server"`, `peer`, `method`, `path`, `action` (Empty → recorded post-inner from the contract `Action::as_str`), `tier` (Empty → recorded inside the inner once `classify_tier` resolves it). Splitting wrapper/inner is required because `#[instrument]`'s span is only active during the function body — the call site is in the parent scope. **(2) Audit mutation entry** — `AuditedMutate::apply` now `#[tracing::instrument]`'d with `otel.kind="internal"` + `method` / `resource` / `action` / `actor` / `request_id` + `outcome` (Empty until the caller folds the success/failure into the chain). Captures the audit-mutated CRUD round-trip on every PUT/DELETE that flows through the dashboard. **(3) Upstream-call span coverage** — `forward_allow_to_upstream` instrumented with `otel.kind="client"` + `host`, `path`, `method`, `upstream`, `member`, `outcome`. `host` recorded after Host-header parse; `upstream` recorded after route resolve; `member` recorded after `pool.strategy.pick`; `outcome` recorded on every terminal branch (no-route / circuit-open / unknown-upstream / no-healthy-upstream / body-read-error / ok / upstream-5xx / upstream-timeout / upstream-unreachable / upstream-error). **Pre-existing clippy lints fixed** to keep `cargo clippy -p aegis-proxy -p aegis-control --lib -- -D warnings` green: `RESERVED_RULE_IDS.iter().any(|r| *r == trimmed)` → `.contains(&trimmed)` (rules.rs); redundant closure in jsonl sink `.map_err(aegis_core::WafError::Io)`; complex tuple type extracted to `BuiltPools` alias in `upstream/registry.rs`; `.iter()` → `.values()` for the validate-pass loop; two `path["/api/upstreams/pool/".len()..]` manual prefix-strips replaced with `.strip_prefix(...)`; one `from_config(&cfg)` (already `&WafConfig`) needless borrow stripped. **Build matrix:** default + `--features otel` both clean; clippy clean on both crates with `-D warnings`. Total **aegis-control 828 / 0 / 0**, **aegis-proxy 393 / 0 / 0** — no regressions. **Operator-visible:** Jaeger now shows `waf.handle_data_request` → `waf.forward_upstream` parent-child trees (one row per data-plane request) plus `audit.mutate.apply` siblings on every dashboard mutation, with attributes filterable in the Jaeger UI. The Grafana exemplar cross-link from `waf_request_duration_ms` → Jaeger trace ID lights up immediately on first traffic. | aegis-control, aegis-proxy | 2026-05-01 |
| **ETCD-T1 (config-from-etcd loader)** — Closes the audit-derived gap "etcd-backed config (currently YAML only; etcd feature is SD only)". New module `aegis-proxy/src/config_source/etcd_source.rs` (~700 lines incl. 19 unit tests) gated under the existing `etcd` Cargo feature so reqwest+base64 are pulled in via the same dep set used by `sd::etcd`. **Wire shape:** the etcd value at `key` (default `/aegis/config/waf`, matching the design in `deploy/etcd/README.md`) is the same YAML blob the file loader accepts — verbatim. Loader fetches via `/v3/kv/range` (single-key range `[key, key+1)`), base64-decodes the value, and feeds through `aegis_core::load_config_str` so the same validation runs. **Auth + TLS** reuse the existing `sd::etcd` env-var pattern: per-source overrides `AEGIS_CONFIG_ETCD_USER` / `_PASSWORD` / `_CA_CERT_PATH` / `_CLIENT_CERT_PATH` / `_CLIENT_KEY_PATH` take precedence over the shared `AEGIS_ETCD_*` fallback so an operator pointing SD at one cluster can point config at another. Token auth via `/v3/auth/authenticate` + `Authorization` header, mTLS via reqwest `Identity::from_pem`. **Boot wiring:** new `resolve_config_source()` in `aegis-bin/src/main.rs` reads `AEGIS_CONFIG_SOURCE` (`file` default, `etcd` opt-in); `etcd` path builds a single-thread bootstrap tokio runtime to do the async fetch before the real `runtime:`-shaped runtime is built from the loaded config. Without `--features etcd`, `AEGIS_CONFIG_SOURCE=etcd` errors at boot with an actionable rebuild message (no silent fallback). **Pure-lookup test pattern:** `from_lookup<F: Fn(&str) -> Option<String>>` decouples env-var resolution from `std::env::var`, so tests don't mutate process-wide env (which races with `sd::etcd::tests`'s `ENV_LOCK` since they touch the same `AEGIS_ETCD_*` vars). `from_env()` is a thin wrapper. 19 pure unit tests cover defaults, override-precedence, fallback, empty-string-as-unset, comma-sep parsing, poll-interval parsing, key default/override, range-response decoding (happy / missing kvs / empty array / bad base64 / invalid JSON), prefix-to-range-end, error display, build_client (success + unreadable CA), `first_non_empty` (skip empty + all-unset). **Watcher (dormant):** `spawn_watcher` polls every `poll_interval`, atomic-swaps the `Arc<ArcSwap<WafConfig>>` on value-changed-and-validates, emits `AuditClass::Admin` `config_reload` / `config_reload_failed` events on the bus. Activation is gated on the broader `aegis-proxy::run` change to take `Arc<ArcSwap<WafConfig>>` instead of `Arc<WafConfig>` — same gating that holds back the file-based `spawn_config_watcher` (existing supervisor.rs). Documented in the new doc section as "follow-up alongside file-watcher reload". **Docs:** `deploy/etcd/README.md` § "Loading config from etcd (ETCD-T1)" added with env-var table + worked example + `validate` round-trip via `bootstrap.sh --show`. **Builds + tests:** `cargo build -p aegis-bin --features etcd` clean; `cargo build -p aegis-bin` (default) clean; clippy `-D warnings` green on aegis-proxy with etcd feature. **aegis-proxy 393 → 412 lib tests** (+19 from config_source) under default features; **430** with `--features etcd`. aegis-control 828 / 0 / 0, aegis-bin 41 / 0 / 0 (+4 from `resolve_config_source` tests), aegis-security 885 / 0 / 0 — no regressions. **Audit gap "etcd-backed config" → CLOSED** for the bootstrap path; live hot-reload deferred to the `Arc<ArcSwap>` runtime plumbing slice. | aegis-proxy, aegis-bin, deploy/etcd | 2026-05-01 |
| **CC-T (compliance-on-reload + boot)** — Closes the audit-derived gap "Compliance clamp on hot-reload — `apply_snapshot_with_compliance` re-runs the clamp on detector-mask PUT (DURABLE-T2), but the hot-reload path on YAML config changes does not yet re-run the clamp on the new mask." Found a second sibling gap during implementation: even **boot-time** without a persistence snapshot skipped the clamp — `cfg.detectors.<class>.enabled: false` paired with `cfg.compliance.modes: [pci]` would silently bypass PCI / HIPAA / SOC2 / GDPR until the next PUT. Fixed both. **New helper** `aegis-control::api::detectors_persist::apply_live_mask_with_compliance(mask, modes)` mirrors the existing `apply_snapshot_with_compliance` but operates on the live `SharedDetectorMask` state directly (no snapshot in scope). Reuses the private `clamp_mask_force_on` so violation messages are identical (`"sqli"` for base, `"override[medium]:sqli"` for per-tier). Returns the same `ApplyOutcome::Applied | AppliedWithCompliance { forced }` shape so callers handle both rehydrate + boot + hot-reload uniformly. Skips the ArcSwap store when no violations were found, keeping hot reads uncontended for the common-case operator who configured a compliant mask in the first place. **Boot-time wire** in `aegis-proxy::run`: after `SharedDetectorMask::from_config(&cfg.detectors)` and before the snapshot-load attempt, run `apply_live_mask_with_compliance` against the cfg-derived initial state. The `compliance_modes` resolution moved up to be shared across both passes (was previously re-derived inside the snapshot-load block). When the snapshot path then runs `apply_snapshot_with_compliance`, the same modes flow through — no behaviour change for the snapshot branch. **Hot-reload wire** in `aegis-proxy::supervisor::spawn_config_watcher` + `watch_loop`: signature gains `detector_mask: Option<SharedDetectorMask>` (None preserves legacy "swap cfg only" behaviour for tests that boot DashboardServices standalone). On every successful `load_config` reload the supervisor (1) re-derives the base mask from `new_cfg.detectors` via `DetectorMask::from_config` + `mask.store(new_base)` — note: per-tier overrides set by PUT /api/detectors are intentionally preserved, since they're explicit operator intent separate from cfg defaults; (2) runs `apply_live_mask_with_compliance` against the new modes; (3) emits a NEW `compliance_clamp_applied` audit event (`AuditClass::Admin`) carrying the `forced` list + `modes` so dashboard operators see exactly what the clamp force-enabled. **The clamp emits its own audit event independently of the existing `config_reload` event** so a hot-reload that triggered a clamp produces two events — one for the cfg swap, one for the compliance correction. **`spawn_config_watcher` is still dormant from the runtime side** (it's called only from supervisor.rs's own tests; `aegis-proxy::run` doesn't yet wrap cfg in `Arc<ArcSwap<WafConfig>>`), so the hot-reload clamp lands as a tested-but-not-yet-activated correctness fix that activates at the same time as the broader cfg-hot-reload plumbing — same gating that holds back ETCD-T1's `spawn_watcher`. **+7 unit tests:** 6 in `detectors_persist::tests` (`live_apply_no_op_when_modes_empty`, `live_apply_no_op_when_mask_already_compliant`, `live_apply_forces_locked_classes_back_on_under_pci`, `live_apply_preserves_unrelated_disables`, `live_apply_clamps_per_tier_overrides`, `live_apply_does_not_store_when_no_violations`) covering the helper directly; 1 in `supervisor::tests::hot_reload_clamps_compliance_locked_class` end-to-end through the file-watcher pipeline (boot mask → write `compliance.modes: [pci]` + `detectors.sqli.enabled: false` → assert mask sqli forced back on + both audit events present in the chain). Total **aegis-control 834 / 0 / 0** (+6); **aegis-proxy 394 / 0 / 0** (+1) under default features, **431 / 0 / 0** with `--features etcd`. Clippy `-D warnings` green on both libs. **Audit gap "Compliance clamp on hot-reload" → CLOSED.** Boot-time silent-bypass gap (sibling) → CLOSED. **Out of scope (deferred):** broader `compliance::apply` boot-time invocation — currently `compliance::apply` runs only in `cmd_validate`, so non-detector adjustments (audit retention, TLS provider, PII pseudonymize) aren't enforced at runtime boot. Different blast radius (touches every cfg field PCI/FIPS/etc. profile rewrites); separate slice. | aegis-control, aegis-proxy | 2026-05-01 |
| **CC-T (config hot-reload plumbing)** — Closes the audit-derived "Config hot-reload plumbing (file + etcd)" gap and activates yesterday's CC-T compliance-clamp-on-reload from "dormant" to "live". **`aegis_proxy::run` signature change:** parameter `cfg: Arc<WafConfig>` → `cfg_swap: Arc<arc_swap::ArcSwap<WafConfig>>` + new `reload_source: ConfigReloadSource` param. Boot snapshot via `let cfg = cfg_swap.load_full()` immediately at the top so every existing internal read site keeps `cfg: Arc<WafConfig>` unchanged — zero touch on the proxy hot path; per-handler reads of `cfg.routes` / `cfg.upstreams` / `cfg.rate_limit` etc. remain boot-snapshotted. **`ConfigReloadSource` enum:** `None` (tests + one-shot CLI), `File(PathBuf)`, `Etcd(EtcdConfigSource)` (cfg-gated under `etcd` feature). **Shared on-reload helper** `aegis-proxy::config_source::reload::apply_cfg_change_to_mask(new_cfg, mask)` — re-derives `DetectorMask::from_config(&new_cfg.detectors)` (preserves per-tier overrides set via PUT /api/detectors), runs the compliance clamp via `apply_live_mask_with_compliance`, returns `ReloadOutcome::{Applied, AppliedWithCompliance { forced }, NoMask}`. Both watchers (`supervisor::watch_loop` for files, `etcd_source::watch_loop` for etcd) now call the shared helper instead of duplicating the clamp logic. **`etcd_source::spawn_watcher` signature update:** gains `detector_mask: Option<SharedDetectorMask>` to mirror `supervisor::spawn_config_watcher`'s shape; both watchers emit `compliance_clamp_applied` audit events on the bus when the clamp had to force classes back on. **aegis-bin wiring** in `run_gateway_inner`: wraps the loaded `WafConfig` in `Arc::new(arc_swap::ArcSwap::from(cfg))` and resolves `ConfigReloadSource` from `AEGIS_CONFIG_SOURCE` via new `resolve_reload_source(config_path)` helper — `file` source becomes `ConfigReloadSource::File(path)`, `etcd` source becomes `ConfigReloadSource::Etcd(EtcdConfigSource::from_env())`. Single `arc-swap` workspace dep added to `aegis-bin/Cargo.toml`. **JoinHandle drop:** the watchers run for the lifetime of the process; `std::mem::drop(spawn_*(..))` (not `let _ =`) is the correct disposal pattern — clippy's `let_underscore_future` lint catches the alternative. Tokio keeps the spawned task alive on the runtime regardless of handle ownership. **What's now active:** edit waf.yaml or push a new value to `/aegis/config/waf` → within 100 ms (file, debounced) or 5 s (etcd, polled) the data plane sees the new cfg in `cfg_swap`, the detector base mask re-derives from `new_cfg.detectors`, the compliance clamp re-runs against `new_cfg.compliance.modes`, and TWO audit events land on the bus: `config_reload` + (if clamp forced classes) `compliance_clamp_applied`. **What's still boot-snapshotted (honest disclosure):** per-request handlers read `cfg.routes` / `cfg.upstreams` / `cfg.rate_limit` etc. from the boot `Arc<WafConfig>` snapshot — they don't yet `cfg_swap.load()` per request. So routes / upstreams / limits will NOT change on hot-reload until a follow-up plumbs `cfg.load()` calls into the per-handler hot path. The `PoolRegistry` (CC-T1.1.b) already provides hot-swap for upstreams via the audit-mutated PUT path; equivalent hot-reload-from-cfg plumbing is the next slice. **Tests +5** in new `config_source::reload::tests` (`no_mask_returns_no_mask_outcome`, `applied_when_no_compliance_modes_and_no_violations`, `rederives_base_from_new_cfg`, `forces_pci_locked_class_back_on`, `preserves_per_tier_overrides_on_reload`). Existing `supervisor::tests::hot_reload_clamps_compliance_locked_class` end-to-end test verifies the file path through the full pipeline (boot mask → write `compliance.modes: [pci]` + `detectors.sqli.enabled: false` → file watcher fires → mask sqli forced back on + both audit events present in the chain). **Build matrix:** default + `--features etcd` + `--features production` all clean. Clippy `-D warnings` green on aegis-control + aegis-proxy + aegis-bin. **Test counts:** aegis-control **834 / 0 / 0** (unchanged), aegis-proxy **399 / 0 / 0** default (was 394, +5 from reload module) / **436 / 0 / 0** with etcd (was 431, +5), aegis-bin 41 / 0 / 0 (unchanged), aegis-security 885 / 0 / 0 (unchanged). **Audit gap "Config hot-reload plumbing"** → CLOSED for the detector-mask + compliance + audit-event slice. The "live route/upstream hot-reload" slice is queued as a follow-up. | aegis-proxy, aegis-bin, aegis-control | 2026-05-01 |
| **CC-T (per-handler hot-reload — routes)** — Closes the audit-derived "Per-handler hot-reload" gap for the loudest surface (routes). After yesterday's CC-T (config hot-reload plumbing) wired the watcher → ArcSwap path, route edits were still ignored at runtime because the per-request handlers read `ctx.route_table` from the boot snapshot. **`RouteTable` refactor:** internal newtype `Arc<ArcSwap<CompiledRouteTable>>` (mirrors the `PoolRegistry` pattern from CC-T1.1.b). Same external API — `RouteTable::build(cfg)`, `resolve(host, path, method)` — plus new `RouteTable::apply(cfg) -> Result<(), WafError>`. Hot-path adds one `ArcSwap::load` per request (~5 ns). In-flight requests that already grabbed an `Arc<CompiledRouteTable>` finish on the old table; new requests after the swap see the new one. Cheap clone — internals are `Arc`. **`RouteTable::apply` semantics:** validation (`CompiledRouteTable::build`) runs FIRST. On failure the old table stays live and `Err(WafError::Config)` is returned — operator sees a `routes_reload_failed` audit event but the data plane keeps serving the previous routes. On success the new table replaces the old via atomic ArcSwap. **New helper** `aegis-proxy::config_source::reload::apply_cfg_change_to_routes(new_cfg, ctx)`. Mirrors the shape of `apply_cfg_change_to_mask`: takes `Option<&Arc<ProxyContext>>` (None for tests/CLI without a live data plane), returns `RouteReloadOutcome::{NoCtx, Applied, Failed { reason }}`. Pools are NOT rebuilt here — they have their own audit-mutated PUT path (CC-T1.1.b) which would race the cfg-driven reload; documented intent. **Both watchers** now call the route helper alongside the mask helper: `supervisor::watch_loop` (file) and `etcd_source::watch_loop` (etcd). Same source-tagged audit fields (`{"source": "file", "path": ...}` vs. `{"source": "etcd", "key": ...}`). Validation failure emits a separate `routes_reload_failed` event with `reason` so the dashboard can surface the rebuild error without colliding with the existing `config_reload` / `config_reload_failed` events. **Watcher signature update:** both `spawn_config_watcher` and `etcd_source::spawn_watcher` gain an `Option<Arc<ProxyContext>>` parameter. `aegis-proxy::run` now defers the watcher spawn until AFTER `upstream_ctx` is built so it can pass `Some(upstream_ctx.clone())` — earlier the spawn block sat between the mask-rehydrate and the proxy-context-build steps, which was a latent ordering bug for this exact wire-up. The boot-time clamp doesn't depend on `upstream_ctx`, so moving the watcher spawn down doesn't delay it. **+8 tests:** 3 in `route::tests` covering `apply` (atomic-swap, validation-error keeps old table, cheap-clone shares inner Arc); 3 in `config_source::reload::tests` covering the new helper (no-ctx outcome, atomic swap, keep-old-on-validation-error); 2 end-to-end `supervisor::tests` (`hot_reload_swaps_route_table` writes a new YAML and asserts /api/v1 stops resolving while /api/v2 starts; `hot_reload_routes_failed_emits_audit_event` writes a no-catch-all YAML and asserts the live route table stays intact + `routes_reload_failed` event lands on the bus). **Bonus: pre-existing rustls flake fix.** `proto::h2::tests::auto_builder_serves_h2_over_tls` was occasionally failing parallel runs due to a rustls `CryptoProvider::install_default()` race — rustls 0.23 exposes both `ring` and `aws-lc-rs` features and refuses to auto-pick a default, so multiple tests calling `ServerConfig::builder()` raced on the install. New `ensure_crypto_provider()` test helper using `std::sync::Once` installs the ring provider exactly once and silently ignores the `Err(_)` from any subsequent caller. **5/5 stable parallel runs** after the fix. **Build matrix:** default + `--features etcd` + `--features production` all clean. Clippy `-D warnings` green on aegis-control + aegis-proxy. **Test counts:** aegis-proxy **407 / 0 / 0** default (was 399, +8) / **444 / 0 / 0** with `--features etcd` (was 436, +8); aegis-control **834 / 0 / 0** unchanged; aegis-bin **41 / 0 / 0** unchanged; aegis-security **885 / 0 / 0** unchanged. **Operator-visible:** edit `routes:` in waf.yaml → within ~100 ms (file) or ~5 s (etcd) the data plane resolves new paths to new upstreams without restart. Combined with CC-T1.1.b's `PoolRegistry` (audit-mutated PUT for pools), operators now have full hot-reload coverage for routes + upstreams + detector mask + compliance clamp + audit chain visibility — the four loudest cfg-driven surfaces. **Honest disclosure (still boot-only):** `cfg.rate_limit.buckets` (IpRateLimiter built once at boot), `cfg.risk` thresholds (RiskTracker built once), `cfg.tls.certificates` (rustls acceptor built once), `cfg.listeners` (tokio binds at boot, restart-only by design), `cfg.runtime` (worker count is builder-time, restart-only). Remaining hot-reload follow-ups are smaller and less operator-loud than routes were. | aegis-proxy, aegis-control | 2026-05-01 |
| **CC-T (per-handler hot-reload — rate-limit)** — Closes the next operator-loud cfg-driven hot-reload surface after routes. Same pattern as the route hot-reload slice: ArcSwap-backed config + atomic-swap method + shared `apply_cfg_change_to_*` helper + wire into both file + etcd watchers + end-to-end test. **`IpRateLimiter` refactor** in aegis-security: `Inner.cfg: IpRateLimitConfig` → `Inner.cfg: ArcSwap<IpRateLimitConfig>`. Hot-path `consume_at` reads via `self.inner.cfg.load()` (one ArcSwap load per request, ~5 ns — strictly cheaper than the surrounding `DashMap::entry` lookup). New `IpRateLimiter::set_config(&self, IpRateLimitConfig)` atomic-swaps the cfg via interior mutability. **Per-IP timestamp state preserved across the swap** — operators editing `cfg.rate_limit.buckets` don't accidentally reset every flooding source IP back to zero counts (would let attackers win via config thrash). New limit applies on next `consume_at`; new window changes which timestamps the eviction loop drops on next call. **New helper** `apply_cfg_change_to_rate_limit(new_cfg, limiter)` returns `RateLimitReloadOutcome::{NoLimiter, Applied { limit, window_secs }, Unchanged}`. The `Unchanged` path skips the `ArcSwap` store on byte-identical configs to keep hot reads uncontended for the common no-op case. **New shared helper** `derive_ip_rate_cfg(cfg)` (pure) — selects the FIRST `scope: Global, key: Ip` bucket (or library default), used by both `aegis-proxy::run` (boot) and the watchers (hot-reload) so the selection rule lives in one place. **Both watchers** (`supervisor::watch_loop` for files, `etcd_source::watch_loop` for etcd) gain a new `ip_rate_limiter: Option<Arc<IpRateLimiter>>` parameter and call the helper after the route reload. Successful swap emits a `rate_limit_reloaded` audit event with `{limit, window_secs, source}` fields so the dashboard can surface the change. **`aegis-proxy::run` wiring**: passes `Some(ip_rate_limiter.clone())` to both `supervisor::spawn_config_watcher` and `etcd_source::spawn_watcher` — the limiter is built well before `upstream_ctx` (the deferred-spawn anchor) so it's already in scope when the watcher gets spawned. **+9 tests:** 3 in `rate_limit::ip_limiter::tests` (`set_config_swaps_limit_atomically` exercises the consume / set / consume sequence; `set_config_preserves_per_ip_timestamp_state` confirms map intact; `set_config_to_lower_limit_denies_already_over_quota_ips` covers the tight-cfg edge case); 5 in `config_source::reload::tests` (`rate_limit_no_limiter_returns_no_limiter_outcome`, `rate_limit_applies_when_changed`, `rate_limit_unchanged_when_cfg_identical`, `rate_limit_falls_back_to_default_when_no_global_ip_bucket`, `rate_limit_preserves_per_ip_state_across_reload`); 1 end-to-end `supervisor::tests::hot_reload_swaps_ip_rate_limit_cfg` writes new YAML and asserts limiter sees swap + audit event lands. **Build matrix:** default + `--features etcd` + `--features production` all clean. Clippy `-D warnings` green on aegis-control + aegis-proxy. **Test counts:** aegis-proxy **413 / 0 / 0** default (was 407, +6) / **450 / 0 / 0** with `--features etcd` (was 444, +6); aegis-security **888 / 0 / 0** (was 885, +3); aegis-control 834, aegis-bin 41 (unchanged). **3 consecutive stable parallel runs** post-fix. **Operator-visible:** edit `cfg.rate_limit.buckets[0]` in waf.yaml (limit / window) → live in ~100 ms (file) / ~5 s (etcd) without restart, per-IP counts preserved. Combined with routes + upstreams + detector mask + compliance clamp, the operator-loud cfg surface is now end-to-end hot-reloadable. **Honest disclosure (still boot-only):** `cfg.risk` thresholds (RiskTracker built once), `cfg.tls.certificates` (rustls acceptor built once), `cfg.listeners` (tokio binds at boot, restart-only by design), `cfg.runtime` (worker count is builder-time, restart-only). The TLS cert refactor is the last operator-loud item; everything else is restart-only-by-design. | aegis-proxy, aegis-security, aegis-control | 2026-05-01 |
| **CC-T (per-handler hot-reload — TLS certs)** — Closes the LAST operator-loud cfg-driven hot-reload surface. After this slice every cfg field that *can* hot-reload *does* — remaining boot-only fields (`cfg.listeners`, `cfg.runtime`) are restart-only by tokio's API. **No structural refactor needed:** the existing `crate::listener::tls::DynamicResolver` already wraps `Arc<ArcSwap<CertStore>>` and exposes `swap(&self, new_store: CertStore)`. The boot-time `aegis_proxy::run` was constructing the resolver inside a match arm and dropping the handle — only the final `TlsAcceptor` survived. This slice **lifts the resolver out of the match scope** so the watchers can keep an `Arc<DynamicResolver>` clone for hot-swap. **Boot path refactor:** the TLS construction match now returns `(Option<Arc<TlsAcceptor>>, Option<Arc<DynamicResolver>>)` instead of just the acceptor. The acceptor still wraps the resolver via the rustls ServerConfig's `Arc<dyn ResolvesServerCert>`, so swapping the resolver's inner ArcSwap surfaces immediately on the next handshake — in-flight handshakes that already loaded the old store finish on it. **Watcher-spawn ordering:** moved the watcher-spawn block to AFTER the TLS construction (was sandwiched between `upstream_ctx` and TLS, missing access to `tls_resolver`). **New helper** `apply_cfg_change_to_tls(new_cfg, resolver)` returns `TlsReloadOutcome::{NoResolver, Applied { cert_count }, SkippedEmpty, Failed { reason }}`. **`SkippedEmpty` semantics**: cfg `tls: None` or empty `certificates` is treated as **skip-not-clear** — clearing the live store would crash every `tls: true` listener's next handshake. Operators wanting to disable TLS at runtime need a restart (logged as a known limitation). **`Failed` semantics**: disk read errors / chain validation errors keep the previous store live and emit `tls_reload_failed`; the failure reason flows through to the audit chain so the dashboard can surface bad PEM paths. **Both watchers** (`supervisor::watch_loop` for files, `etcd_source::watch_loop` for etcd) gain a new `tls_resolver: Option<Arc<DynamicResolver>>` parameter and call the helper after the rate-limit reload. Successful swap emits `tls_reloaded` audit event with `cert_count + source` fields. **+5 tests in `config_source::reload::tests`** (`tls_reload_no_resolver_returns_no_resolver_outcome`, `tls_reload_swaps_cert_store_atomically`, `tls_reload_skips_when_new_cfg_has_no_tls`, `tls_reload_fails_on_missing_cert_file`, `tls_reload_failed_keeps_old_resolver_responsive`). **+2 end-to-end `supervisor::tests`** (`hot_reload_swaps_tls_cert_store` writes new cert PEMs + asserts resolver swaps + audit event lands; `hot_reload_tls_failed_keeps_old_certs_live` writes a config pointing at non-existent paths + asserts old certs still resolvable + `tls_reload_failed` event lands). **Build matrix:** default + `--features etcd` + `--features production` all clean. Clippy `-D warnings` green on aegis-control + aegis-proxy. **Test counts:** aegis-proxy **420 / 0 / 0** default (was 413, +7) / **457 / 0 / 0** with `--features etcd` (was 450, +7); aegis-control 834 / aegis-bin 41 / aegis-security 888 unchanged. **3 consecutive stable parallel runs.** **Operator-visible:** rotate `cfg.tls.certificates[*].cert_path` / `key_ref` in waf.yaml → live in ~100 ms (file) or ~5 s (etcd) without restart, in-flight handshakes complete on the old store. Combined with routes + upstreams + detector mask + compliance clamp + ip rate-limit, the operator-loud cfg surface is now **end-to-end hot-reloadable** — every cfg-driven setting that can change without restart, does. **What's still boot-only (by design):** `cfg.listeners` (tokio binds at boot, restart-only — there's no API for unbinding), `cfg.runtime` (tokio worker count is builder-time, restart-only). `cfg.risk` thresholds are technically still boot-only too but the audit-mutated `/api/risk/*` endpoints provide an alternative live-control path. | aegis-proxy, aegis-control | 2026-05-01 |
| **MTLS-T1 (config schema + identity types)** — Plan + Slice 1 of the new mTLS track. **Plan**: new `plans/mtls.md` documents the full 12-slice roadmap (config / rustls wiring / identity extraction / policy integration / hot-reload / 5-tier console UI / break-glass / docs). Architecture maps the canonical zero-trust pipeline (TLS Termination → Identity Extractor → WAF Policy Engine → Backend) onto the existing aegis-proxy stages — the only missing layer is Identity Extractor as a discrete stage. Outbound mTLS already supported via `UpstreamTlsConfig`; logical SAN-allowlist policy already exists in `aegis-control::admin_auth::mtls` but dormant because the rustls listeners use `with_no_client_auth()`. **Slice 1 (this turn) — types only, no behaviour change**: (1) `aegis-core::config::ClientAuthConfig` added to `TlsConfig` as `client_auth: Option<...>`; defaulting `client_auth: None` keeps the legacy `with_no_client_auth()` path so existing configs are byte-identical at runtime. Fields: `mode: ClientAuthMode {Disabled, Optional, Required}` (snake_case serde, default Disabled), `ca_bundle: Option<PathBuf>`, `allowed_sans: Vec<String>`, `apply_to: Vec<ClientAuthScope {Admin, Data}>` (default `[Admin]` so an opt-in cfg without explicit scope doesn't accidentally break data-plane clients). (2) `validate_tls_hardening` extended to reject `mode != Disabled` without a `ca_bundle` and without a non-empty `apply_to` — `mode: Disabled` keeps the cfg "stagable" so operators can pre-populate fields then flip mode later. (3) New `aegis-core::identity` module with `ClientIdentity` enum (`Anonymous` / `Mtls { san, fingerprint, chain_ok }` / `Spiffe { uri, td }`) plus `kind()` (stable label for audit + Prometheus), `principal()` (display string for audit), `is_authenticated()` helpers. **Enum-not-struct shape choice** documented in the module: lets future variants (JWT / API key / OIDC) land as new arms with compiler-forced exhaustive-match fix-ups at the policy decision sites — that's the design goal. **+18 tests:** 7 in `identity::tests` (default is Anonymous, kind labels stable, principal returns expected per variant, anonymous not-authenticated, mtls follows chain_ok, spiffe always authenticated, clone has value semantics) + 11 in `config::tests::client_auth_*` (absent → default disabled, disabled mode doesn't require ca_bundle, optional/required round-trips, apply_to default is `[admin]`, apply_to admin+data, required without ca_bundle rejected, optional without ca_bundle rejected, required + empty apply_to rejected, unknown mode rejected at deserialise, unknown scope rejected at deserialise). **Build matrix:** default + `--features etcd` + `--features production` all clean. Clippy `-D warnings` green on aegis-core + aegis-control + aegis-proxy. **Test counts:** aegis-core **163 / 0 / 0** (was 145, +18); aegis-proxy 420 default / 457 etcd, aegis-control 834, aegis-bin 41, aegis-security 888 — **all unchanged** because Slice 1 is types-only. **Next up: MTLS-T6 (read-only console observability) lands BEFORE MTLS-T2's rustls wiring** so the dashboard surface is built and visible before the actual handshake change goes live — operators see "what data this page will show" first, then T2 fills it in. | aegis-core, plans, docs/progress | 2026-05-01 |
| **MTLS-T6 (read-only console observability — backend)** — Per `plans/mtls.md`, ships the Tier-1 read-only mTLS observability surface BEFORE MTLS-T2's rustls handshake change so operators see what data the dashboard will show first. **New module `aegis-control::identity_tracker`** (~600 lines incl. 12 unit tests). `IdentityTracker` is sibling-shaped to `RiskTracker`: `DashMap<String, IdentityEntry>` keyed by principal (SAN for mtls, URI for spiffe, `"anonymous"` for the catch-all), each entry carrying a sliding `request_count` + per-decision `DashMap<&'static str, AtomicU64>` + `last_seen` timestamp. **Hot-path `record_request`** is wait-free per shard; **`record_failure`** captures handshake failures with a `MAX_FAILURE_REASONS=32` cap to prevent memory blow-up if an operator-supplied error string ever reaches it. Idle entries reaped on a 60s `IDLE_SWEEP_INTERVAL` gate. **CA bundle parser** `parse_ca_bundle(&Path) -> Result<CaSummary, CaParseError>` reuses the existing `rustls_pemfile` + `x509_parser` crates already in the workspace; extracts `subject` + `not_before/after` + SHA-256 fingerprint per cert. **CA summary served immediately** (boot path parses the bundle if `cfg.tls.client_auth.ca_bundle` is set) — operators can validate their CA path + cert expiry BEFORE flipping `mode: required`. **Snapshot DTOs** (`IdentitySnapshot`, `FailureSnapshot`, `CaSummary`, `CaCertSummary`) are `Serialize` so the api/mtls handlers serialise directly. **New `aegis-control::api::mtls`** (~330 lines incl. 9 tests). Four GET render functions: `MtlsConfigView::from_config(cfg).render()` reads `cfg.tls.client_auth` (with sensible default-disabled fallback when None), `render_connections / render_failures / render_ca_summary` take `Option<&Arc<IdentityTracker>>` so the handler serves empty-state bodies when the tracker isn't wired (matches the alert-receivers / upstreams pattern — no 404 path on missing service handle). The cfg view's `active: bool` is **always `false` in this slice** — MTLS-T2 will populate it from the live `WebPkiClientVerifier` state. **DashboardServices** gains `identity_tracker: Option<Arc<IdentityTracker>>` field; constructor seeded with `None`, proxy boot path sets it. **aegis-proxy dispatch wiring** in `lib.rs` adds 4 new GET arms (`/api/mtls`, `/connections`, `/failures`, `/ca-summary`); boot path constructs the tracker, parses the CA bundle if configured, calls `set_ca_summary`, and stashes the tracker into `services.identity_tracker`. **+21 aegis-control tests** (12 identity_tracker: empty/record/sort-desc/window-eviction/failure-aggregation/cap-overflow/CA-roundtrip/clear-with-none/parse-extracts/parse-empty-PEM/parse-IO-error/cheap-clone; 9 api::mtls: cfg-default-disabled/cfg-required-renders/cfg-renders-JSON/connections-none-empty-state/connections-some-snapshot/failures-none/failures-some/ca-summary-none/ca-summary-with-metadata). **Build matrix:** default + `--features etcd` + `--features production` all clean. Clippy `-D warnings` green on aegis-core + aegis-control + aegis-proxy. **Test counts:** aegis-control **855 / 0 / 0** (was 834, +21); aegis-proxy 420 / 457 unchanged (no proxy-side test additions in this slice — the wiring is non-test code); aegis-core 163, aegis-bin 41, aegis-security 888 unchanged. **What works today (no MTLS-T2 needed):** dashboard's `/api/mtls` returns the cfg snapshot; `/api/mtls/ca-summary` returns parsed cert metadata if `ca_bundle` is configured. **What's empty until MTLS-T2 / T3:** `/connections` / `/failures` — no caller invokes `record_request` / `record_failure` until rustls + identity-extraction land. The dashboard frontend (MTLS-T6 frontend) is **deferred to a follow-up** because adding handler wiring + a dashboard page to `aegis-proxy/src/lib.rs` (currently 5569 lines, well past the 800-line guideline) would push it past 5700 lines. Operator feedback flagged this — next slice is **PRE-T1 (proxy lib.rs split)** before more handlers go in. | aegis-control, aegis-proxy | 2026-05-01 |
| **PRE-T1 (extract `responses.rs`)** — First slice of the proxy refactor (`plans/proxy-refactor.md`). Pure structural extraction, zero behaviour change. Six response-builder helpers moved out of `aegis-proxy/src/lib.rs` into a new `responses.rs` submodule: `json_response`, `json_body_response`, `apply_dashboard_security_headers`, `dashboard_response`, `dashboard_shell_response`, `mutation_error_response`. All `pub(crate)` — internal-only — with a `use responses::{...}` line in `lib.rs` so the **166 call sites stay byte-identical**. **Doc comments preserved** (the existing per-fn docs moved with the code; new module-level doc explains the extraction rationale). **+4 unit tests** in `responses::tests`: `json_response_serialises_value` (round-trip + content-type assertion), `json_body_response_uses_provided_cache_control` (cache-control header threads through), `json_response_falls_back_when_value_unrepresentable` (pins the `"{}"` infallibility contract), `apply_dashboard_security_headers_adds_table_headers` (every `SECURITY_HEADERS` entry surfaces on the response). **File sizes:** `aegis-proxy/src/lib.rs` 5569 → **5484 lines** (−85); `responses.rs` 213 lines (well under the 800-line guideline). **Build matrix:** default + `--features etcd` + `--features production` all clean. Clippy `-D warnings` green. **Test counts:** aegis-proxy **424 / 0 / 0** default (was 420, +4) / **461 / 0 / 0** etcd (was 457, +4); aegis-control 855, aegis-core 163, aegis-bin 41, aegis-security 888 — **all unchanged**. **3 consecutive stable parallel runs** confirm no regression in the rustls + supervisor end-to-end paths. **What's next**: PRE-T2 (extract `data_plane.rs` — `handle_data_request` + `forward_allow_to_upstream`, ~700 lines) is the next slice. Each subsequent PRE-T slice continues to extract a focused submodule until `lib.rs` ≤ 200 lines (the PRE-T8 success criterion). | aegis-proxy | 2026-05-01 |
| **PRE-T2 (extract `data_plane.rs`)** — Second slice of the proxy refactor (`plans/proxy-refactor.md`). Pure structural extraction, **zero behaviour change**. Four functions moved out of `aegis-proxy/src/lib.rs` into a new `data_plane.rs` submodule: `handle_data_request` (the `#[tracing::instrument]` wrapper that owns the OTEL-T3 root span and records `action`), `handle_data_request_inner` (the actual per-request logic — rate-limit → tier → detectors → risk → forward/block), `forward_allow_to_upstream` (route resolve + circuit breaker + pool member pick + body forward), and `blocked_response` (the small private helper every block path uses to build the 403 + emit the audit-chain entry). **Visibility:** the three entry points are `pub(crate) async fn`; `blocked_response` stays private (only used inside the module). Call site in `lib.rs::accept_loop` rebound via `use data_plane::handle_data_request;` so it stays byte-identical. **No tests moved** — the data-plane tests live in `lib.rs::tests::run_binds_and_serves_200` and exercise full proxy boot rather than data-plane internals; they stay attached to `aegis_proxy::run` until PRE-T7 lifts run too. **File sizes:** `aegis-proxy/src/lib.rs` 5484 → **4900 lines** (−584 in this slice, cumulative −669 from the 5569 baseline). `data_plane.rs` 616 lines (under the 800 guideline). `responses.rs` 213 lines (unchanged). **Build matrix:** default + `--features etcd` + `--features production` all clean. Clippy `-D warnings` green. **Test counts:** aegis-proxy **424 / 0 / 0** default and **461 / 0 / 0** etcd (unchanged from PRE-T1 — no test additions since the move is structural). All other crates unchanged: aegis-control 855, aegis-core 163, aegis-bin 41, aegis-security 888. **3 consecutive stable parallel runs.** **What's next**: PRE-T3 (extract `admin/sse.rs`, ~300 lines — `/dashboard/sse` streaming endpoint, well-isolated) is the next slice. Then PRE-T4 (admin/login.rs) → T5 (admin/get_handlers.rs, the big chunk) → T6 (admin/mutation_handlers.rs) → T7 (run.rs) → T8 (verify). | aegis-proxy | 2026-05-01 |
| **PRE-T3 (skip — already extracted) + PRE-T4 (extract `admin_login.rs`)** — Two slices in one turn. **PRE-T3 was a no-op** because `admin_sse.rs` (the SSE streaming module) was already extracted earlier (B4-T4 era; 385 lines). Plan documentation updated to reflect actual state. **PRE-T4 (login extraction)**: pure structural move, **zero behaviour change**. Four functions moved out of `aegis-proxy/src/lib.rs` into a new `admin_login.rs` submodule (151 lines): `handle_admin_login` (async hyper wrapper), `process_admin_login` (pure body — driven by unit tests without faking a TCP stream), `handle_admin_logout`, `process_admin_logout`. All `pub(crate)` so the dispatch in `lib.rs::admin_router` and the existing 5 login/logout unit tests both reach in via `crate::admin_login::...`. **Bonus extraction**: `extract_named_cookie` (12-line shared helper used by 7 call sites — login, logout, CSRF-gated mutations, SSE auth) moved to `responses.rs` so it lives alongside the other shared response helpers. lib.rs's `use responses::{...}` extended to import it. **File sizes:** `aegis-proxy/src/lib.rs` 4900 → **4762 lines** (−138 in this slice; cumulative −807 from the 5569 baseline across PRE-T1 + T2 + T4). `admin_login.rs` 151 lines (new); `responses.rs` 213 → 231 lines (+18 for `extract_named_cookie`); `data_plane.rs` 616 lines unchanged; `admin_sse.rs` 385 lines unchanged. **All 5 modules under the 800-line guideline.** **Test fixup**: lib.rs's `tests` module gains `use crate::admin_login::{process_admin_login, process_admin_logout};` so the 5 login/logout unit tests still compile. **Build matrix:** default + `--features etcd` + `--features production` all clean. Clippy `-D warnings` green. **Test counts:** aegis-proxy **424 / 0 / 0** default / **461 / 0 / 0** etcd (unchanged; pure move). 3/3 stable parallel runs. **What's next**: PRE-T5 (extract `admin_get_handlers.rs`, ~1200 lines — the big chunk: every `GET /api/*` arm) is the next slice. After T5 + T6 + T7, lib.rs will be ≤ 200 lines and the refactor is done. | aegis-proxy | 2026-05-01 |
| **PRE-T5 (extract `admin_get.rs`)** — Fifth slice of the proxy refactor (`plans/proxy-refactor.md`). Pure structural extraction, **zero behaviour change**. The big chunk: `admin_router` (the GET-side dispatcher every admin / dashboard / health / metrics request flows through, ~430 lines covering `/healthz/*`, `/metrics`, `/api/about`, `/api/stats`, `/api/timeseries`, `/api/audit/since`, `/api/rules*`, `/api/upstreams*`, `/api/risk*`, `/api/mode`, `/api/loadmode`, `/api/runtime`, `/api/detectors`, `/api/blacklist`, `/api/whitelist`, `/api/alerts`, `/api/slo`, `/api/certs`, `/api/cluster`, `/api/gitops/*`, `/api/mtls*` (MTLS-T6), `/api/filters`, `/api/integrations`, `/api/admin/*`, `/api/cold-tier`, `/api/logging`, `/api/analytics/*`, `/api/threat-intel/*`, `/api/bots/*`, `/api/audit/witness`, `/api/tracking/*`, `/api/upstreams/config`) plus the three small `?key=value` query parsers (`parse_query_u32`, `parse_query_str`, `parse_query_u64`) moved alongside as private helpers. **Visibility:** `admin_router` is `pub(crate)`; the three parsers stay private (only used inside the module). Single call site in `lib.rs::admin_listener`'s service_fn rebound via `use admin_get::admin_router;` — byte-identical. **Module-shuffling cleanup**: the lib.rs-level `use responses::{...}` lost `dashboard_response` + `dashboard_shell_response` (their non-test callers all moved to `admin_get.rs` along with `admin_router`). The 5 dashboard-response unit tests in `lib.rs::tests::dashboard_*` import them via `use crate::responses::{dashboard_response, dashboard_shell_response};` — same shape as the PRE-T4 login-test fixup. **File sizes (cumulative across PRE-T1..T5):** | `aegis-proxy/src/lib.rs` | 5569 → **4287** lines (cumulative −1282, −23%) | `responses.rs` | 213 → 231 (PRE-T4 added `extract_named_cookie`) | `data_plane.rs` | 616 (PRE-T2) | `admin_login.rs` | 151 (PRE-T4) | `admin_sse.rs` | 385 (pre-existing) | `admin_get.rs` (new) | **519 lines** | All 6 submodules under the 800-line guideline. lib.rs still over (4287); PRE-T6 (mutation handlers, ~1500 lines) + PRE-T7 (run.rs, ~700 lines) will finish. **Build matrix:** default + `--features etcd` + `--features production` all clean. Clippy `-D warnings` green. **Test counts:** aegis-proxy **424 / 0 / 0** default / **461 / 0 / 0** etcd (unchanged; pure move). **3/3 stable parallel runs.** **What's next**: PRE-T6 (extract `admin_mutate.rs`, ~1500 lines — every audit-mutated PUT/POST/DELETE handler) is the next slice. After T6 + T7, lib.rs will be ≤ 200 lines and the refactor is done. | aegis-proxy | 2026-05-01 |
| **PRE-T6 (extract `admin_mutate.rs`)** — Sixth slice of the proxy refactor (`plans/proxy-refactor.md`). The biggest chunk yet. Pure structural extraction, **zero behaviour change**. **18 audit-mutated handlers + 5 supporting helpers** moved out of `aegis-proxy/src/lib.rs` into a new `admin_mutate.rs` submodule (1714 lines): `handle_mode_put`, `handle_upstreams_config_put`, `handle_pool_upsert`, `handle_pool_delete`, `handle_alert_receivers_put`, `handle_alert_receiver_delete`, `handle_alert_receiver_test`, `handle_alert_ack`, `handle_logging_put`, `handle_loadmode_put`, `handle_rules_post`, `handle_rules_put`, `handle_rules_delete`, `handle_rules_toggle`, `handle_risk_thresholds_put`, `handle_risk_reset`, `handle_detectors_put`, `handle_admin_drain`. Plus shared helpers (`mutation_preamble` + `MutationPreamble` struct, `redact_receivers_for_audit`, `upstreams_audit_view`, `default_true`, `mask_state_to_json`). **Visibility**: all 18 handler entry points `pub(crate) async fn` (sed batch-rewrite); helpers private. lib.rs's `handle_admin_request` rebound via `use admin_mutate::{...18 handlers...};`. **Imports**: admin_mutate.rs gets `std::sync::Arc`, `aegis_core::config::WafConfig`, `aegis_core::ReadinessSignal`, plus `crate::responses::{extract_named_cookie, json_body_response, json_response, mutation_error_response}`. lib.rs's responses-import shrinks to just `{json_body_response, json_response}` (the rest were only used by the moved mutations). **File sizes (cumulative across PRE-T1..T6):** | `aegis-proxy/src/lib.rs` | 5569 → **2650** lines (cumulative **−2919, −52%**) | `responses.rs` | 231 | `data_plane.rs` | 616 | `admin_login.rs` | 151 | `admin_sse.rs` | 385 (pre-existing) | `admin_get.rs` | 519 | `admin_mutate.rs` (new) | **1714** lines (over the 800-line guideline — flagged in the module's own doc-comment as a follow-up split candidate; functionally cohesive so further splitting is judgment call). 6 of 7 submodules under 800 lines. **Build matrix:** default + `--features etcd` + `--features production` all clean. Clippy `-D warnings` green. **Test counts:** aegis-proxy **424 / 0 / 0** default / **461 / 0 / 0** etcd (unchanged; pure move). **3/3 stable parallel runs.** **What's next**: PRE-T7 (extract `run.rs`, the `pub async fn run` boot orchestration ~700 lines) is the next slice. After T7, lib.rs will be a thin facade ~200 lines and PRE-T8 will verify the success criteria. | aegis-proxy | 2026-05-01 |
| **CFG-SYNC Phase 0 (StateBackend generic KV primitives)** — Foundation for the multi-node config plane + counter aggregation (`plans/future/cluster-config-sync-and-scaling.md`). Adds four `StateBackend` trait methods with safe default impls (so the ~20 stub/out-of-tree backends keep compiling): `incrby(key, delta) -> u64` (atomic commutative counter), `expire(key, ttl)` (TTL refresh), `scan_prefix(prefix) -> Vec<String>` (key enumeration), `cas_set(key, expected, new, ttl: Option<Duration>) -> bool` (single-key compare-and-set; `None` ttl persists so config keys don't age out). Implemented in `RedisBackend` (new `CAS_SET_LUA` for atomic CAS + `INCRBY`/`PEXPIRE`/`SCAN`) and `InMemoryBackend` (DashMap entry CAS), and delegated through the `ReconcilingBackend` (config CAS mirrors to fallback on success; fail-static on partition) + `MeteredStateBackend` (forwarded without per-op metric, like `health()`). +6 unit tests (in_memory cas/incrby/scan; core trait default unchanged). Build green with `--features "redis geoip ai affinity"`. Commit `dcdd96f`. | aegis-core, aegis-proxy, aegis-control | 2026-05-27 |
| **CFG-SYNC Phase A core (ConfigStore + redis_source watcher)** — Shared, versioned config document so a console edit syncs to every node and survives leader failover (config lives in the store, not the leader's RAM). `config_source/config_store.rs`: `ConfigStore` over `StateBackend` — single `config:waf:doc` (JSON `{version, blob(YAML), actor, ts, summary}`) activated by optimistic-concurrency CAS (`activate(expected_version,...) -> Applied{version} | Conflict{current}`), immutable per-version snapshots `config:waf:v:<n>` for `rollback`, and per-node `config:waf:applied:<node>` ACK keys (`applied_map` for the console drift view). `config_source/redis_source.rs`: `spawn_watcher` polls the doc version (default 3s) and applies new versions via the existing `reload::` helpers (mask/routes/rate-limit/TLS + `ArcSwap` swap, identical to the file/etcd watchers); ACKs applied version, NACKs (keeps last-good + `config_reload_failed` audit) on validate failure, fail-static when the store is unreachable. Backend-agnostic (in_memory single-node + redis multi-node). +7 unit tests (activation, sequential bump, stale-version conflict, rollback restores old content, applied-map). Build green. Commit `e9691d1`. **Remaining for Phase A**: `PUT /api/config` write endpoint, boot-site `spawn_watcher` in `run.rs`, console drift badge. | aegis-proxy | 2026-05-27 |
| **CFG-SYNC Phase A boot wiring + write path** — Wires the config plane end-to-end. (1) Boot wiring (`e4bc458`): `redis_source::spawn_watcher` spawns in `aegis-proxy::run` on every node alongside the file/etcd watchers, independent of the boot config source, reusing `lease_store.self_id()` as the node identity for the per-node applied-version ACK; harmless no-op on `in_memory`. (2) Write path (`912b16e`): new `AuditedMutate::apply_async` — async sibling of `apply` with the identical contract (CSRF first; future runs once, only after CSRF passes; chain entry iff the future succeeds; std `Mutex` never held across `.await`) chosen as option (a) over manual CSRF/audit. `PUT /api/config` validates the YAML blob (single validation surface via `load_config_str`) → `ConfigStore::activate(expected_version,...)` → 200 `{version}` on apply / 409 `{current}` on optimistic-concurrency conflict; `POST /api/config/rollback` → `ConfigStore::rollback`. Both build a `ConfigStore` from `services.state_backend` (already a `DashboardServices` field — no new field), CSRF-gated + audit-chained; routed in `admin_dispatch.rs`. Activated version then propagates to every node via the shared-store watcher → **edit→store→fleet-converge, survives leader failover**. +3 `apply_async` unit tests. Builds across proxy + full bin feature sets. **Remaining for Phase A**: `GET /api/config` drift view (the `admin_get.rs` GET router is a sync `match` — needs an async arm) + console applied-version badge. | aegis-proxy, aegis-control | 2026-05-27 |
